x=>x.status!=='已确认').length}}
function dateDelta(d){if(!d)return 9999;let a=new Date(day()+'T00:00:00'),b=new Date(d+'T00:00:00');return Math.round((b-a)/86400000)}function nextActions(){let a=[];for(let x of s.defects.filter(x=>x.status!=='已解决'))a.push({icon:'⚠️',title:x.title||'处理 Defect',sub:x.room||x.owner||'待处理',status:'Defect',score:96,due:x.due||''});for(let x of s.decisions.filter(x=>x.status!=='已确认')){let dd=dateDelta(x.due),sc=dd<0?100:dd===0?96:dd<=2?86:72;a.push({icon:'◇',title:x.title,sub:[x.room,x.block?'会阻塞：'+x.block:''].filter(Boolean).join(' · ')||'需要确认',status:dd<0?'已逾期':dd===0?'今天截止':'待确认',score:sc,due:x.due||''})}for(let x of s.tasks.filter(x=>!x.done)){let dd=dateDelta(x.due),sc=dd<0?98:dd===0?94:dd<=2?82:64;a.push({icon:'✅',title:x.name,sub:x.room||x.note||'装修待办',status:dd<0?'已逾期':dd===0?'今天':'待处理',score:sc,due:x.due||''})}for(let x of s.variations.filter(x=>x.status==='待批准'))a.push({icon:'±',title:x.title,sub:(x.room?x.room+' · ':'')+rm(x.amount),status:'待批准',score:80,due:x.due||''});for(let x of s.inspections.filter(x=>x.status!=='通过')){let dd=dateDelta(x.due);a.push({icon:'✓',title:x.title,sub:x.room||'阶段验收',status:dd<0?'验收逾期':'待验收',score:dd<0?92:70,due:x.due||''})}for(let x of s.shopping.filter(x=>['已选','等优惠'].includes(x.status)))a.push({icon:'🛒',title:'待购买：'+x.item,sub:[x.room,x.platform].filter(Boolean).join(' · ')||'已选商品',status:x.status,score:58,due:x.expected||''});return a.sort((x,y)=>y.score-x.score||dateDelta(x.due)-dateDelta(y.due)).slice(0,6)}function readiness(){let room=s.rooms.length?s.rooms.reduce((a,r)=>a+Number(s.roomData[r]?.progress||0),0)/s.rooms.length:0,ins=s.inspections.length?s.inspections.filter(x=>x.status==='通过').length/s.inspections.length*100:room,def=s.defects.length?s.defects.filter(x=>x.status==='已解决').length/s.defects.length*100:100,hand=s.handover.length?s.handover.filter(x=>x.status==='已收齐'||x.status==='完成').length/s.handover.length*100:room;let pct=Math.round(room*.45+ins*.25+def*.15+hand*.15);return Math.max(0,Math.min(100,pct))}
function go(p){$$('.page').forEach(x=>x.classList.toggle('on',x.dataset.page===p));$$('[data-nav]').forEach(x=>x.classList.toggle('on',x.dataset.nav===p));$('#title').textContent={home:'我的装修',rooms:'房间',control:'控制中心',budget:'预算',me:'我的'}[p];scrollTo(0,0)}$$('[data-nav]').forEach(b=>b.onclick=()=>go(b.dataset.nav));$$('[data-go]').forEach(b=>b.onclick=()=>go(b.dataset.go));function empty(t='还没有资料'){return `<div class="empty">${t}</div>`}function pill(t){return `<span class="pill ${/完成|已确认|已批准|通过|已解决/.test(t)?'ok':/超|不通过|逾期/.test(t)?'warn':''}">${esc(t)}</span>`}
function render(){if(!s)return;let t=totals(),avg=s.rooms.length?Math.round(s.rooms.reduce((a,r)=>a+Number(s.roomData[r]?.progress||0),0)/s.rooms.length):0;$('#hprog').textContent=avg+'%';$('#hprogbar').style.width=avg+'%';$('#hn').textContent=s.name;$('#hs').textContent='当前阶段：'+s.stage;$('#hb').textContent=rm(s.budget);$('#hp').textContent=rm(t.actual);$('#ho').textContent=t.open+' 项';$('#pn').textContent=s.name;$('#pm').textContent=[s.type,s.area,s.stage].filter(Boolean).join(' · ');if($('#notionReportText'))$('#notionReportText').textContent=s.notionImport?`已导入 ${s.notionImport.shoppingImported} 商品 · ${s.notionImport.rooms} 房间 · ${s.notionImport.expenses} 支出`:'尚未迁移';let ratio=s.budget?t.final/s.budget:0;$('#risk').classList.toggle('hide',!s.budget||ratio<.8);$('#risk').classList.toggle('danger',ratio>1);$('#risk').textContent=ratio>1?'⚠️ 预计结算已超过预算 '+rm(t.final-s.budget):'预计结算已使用预算 '+Math.round(ratio*100)+'%';let acts=nextActions();$('#homeItems').innerHTML=acts.length?`<div class="nextList">${acts.map(x=>`<button class="nextCard ${/逾期|Defect/.test(x.status)?'urgent':''}" data-go="control"><span class="nextIcon">${x.icon}</span><span class="nextCopy"><b>${esc(x.title)}</b><small>${esc(x.sub)}</small></span><span class="nextMeta"><strong>${esc(x.status)}</strong><small>${x.due?esc(x.due):'未设日期'}</small></span></button>`).join('')}</div>`:empty('目前没有需要你处理的事情');$$('#homeItems [data-go]').forEach(b=>b.onclick=()=>go('control'));let today=s.siteLogs.filter(x=>x.date===day());$('#todayBox').innerHTML=today.map(x=>`<div class="row"><span>📝</span><span class="main"><b>${esc(x.room||'现场日志')}</b><small>${esc(x.work||x.note||'')}</small></span></div>`).join('')||empty('今天还没有施工日志');let sm=shopMoney(),cand=s.shopping.filter(x=>['想买','找资料','比价中'].includes(x.status)).length,sel=s.shopping.filter(x=>['已选','等优惠'].includes(x.status)).length,bought=s.shopping.filter(x=>['已下单','运输中','已到货','已安装','完成'].includes(x.status)).length;$('#shopSummary').innerHTML=`<div><small>候选</small><b>${cand} 件</b></div><div><small>已选</small><b>${sel} 件</b></div><div><small>已购买</small><b>${bought} 件</b></div>`;let rd=readiness();$('#readyRing').style.setProperty('--p',rd);$('#readyPct').textContent=rd+'%';$('#readyTitle').textContent=rd>=90?'已经接近可以入住':rd>=70?'进入收尾阶段':rd>=40?'装修正在推进':'还在前中期';let miss=[];if(s.defects.some(x=>x.status!=='已解决'))miss.push(s.defects.filter(x=>x.status!=='已解决').length+' 个 Defect');if(s.inspections.some(x=>x.status!=='通过'))miss.push(s.inspections.filter(x=>x.status!=='通过').length+' 项验收');if(s.handover.some(x=>x.status!=='已收齐'&&x.status!=='完成'))miss.push(s.handover.filter(x=>x.status!=='已收齐'&&x.status!=='完成').length+' 项交付资料');$('#readyDesc').textContent=miss.length?'当前主要还差：'+miss.join('、'):'继续完成各房间与施工事项，准备度会自动更新。';$('#rooms').innerHTML=s.rooms.map(r=>{let d=s.roomData[r]||{},n=s.tasks.filter(x=>x.room===r&&!x.done).length,df=s.defects.filter(x=>x.room===r&&x.status!=='已解决').length,sp=s.shopping.filter(x=>x.room===r&&!['完成','未选择'].includes(x.status)).length;return `<button class="room" data-r="${esc(r)}"><span class="roomIcon">${EM[r]||'🏠'}</span><span class="roomCopy"><b>${esc(r)}</b><small>${d.progress||0}% · ${n} 待办 · ${df} Defect · ${sp} 选购</small><div class="bar"><i style="width:${d.progress||0}%"></i></div></span><span class="roomArrow">›</span></button>`}).join('')||empty('还没有房间');$$('[data-r]').forEach(b=>b.onclick=()=>roomHub(b.dataset.r));renderControl();renderBudget()}
function updateControlHero(){let sh=tab==='shopping';$('#controlKicker').textContent=sh?'收藏 × 对比 × 购买':'决策 × 执行';$('#controlTitle').textContent=sh?'选购中心':'控制中心';$('#controlDesc').textContent=sh?'把网上看到的商品先收藏，再比较、决定、下单和到货。':'确认、变更、日志、验收、选购统一管理。'}
function renderControl(){updateControlHero();if(tab==='shopping'){renderShoppingHub();return}let a=[];if(tab==='decision')a=s.decisions.map(x=>['◇',x.title,[x.room,x.due?('截止 '+x.due):'',x.block?('阻塞 '+x.block):''].filter(Boolean).join(' · '),x.status||'待确认']);if(tab==='variation')a=s.variations.map(x=>['±',x.title,rm(x.amount)+(x.room?' · '+x.room:''),x.status||'待批准']);if(tab==='log')a=s.siteLogs.map(x=>['📝',x.date,x.room+' · '+(x.work||''),'已记录']);if(tab==='inspect')a=s.inspections.map(x=>['✓',x.title,[x.room,x.due?('计划 '+x.due):''].filter(Boolean).join(' · '),x.status||'待检查']);if(tab==='contractors')a=s.contractors.map(x=>['👷',x.name,[x.type,x.contact,x.quote?('记录金额 '+rm(x.quote)):'',x.deposit].filter(Boolean).join(' · '),x.status||'未开始']);if(tab==='sockets')a=s.sockets.map(x=>['🔌',x.title,[x.room,x.position,x.qty,x.type].filter(Boolean).join(' · '),x.status||'待确认']);if(tab==='handover')a=s.handover.map(x=>['🔑',x.title,[x.vendor,x.expiry?('保固至 '+x.expiry):''].filter(Boolean).join(' · ')||x.note||'',x.status||'待收齐']);let addBtn=tab==='handover'?'<button class="controlAdd" id="addHandover">＋ 添加交付 / Warranty</button>':'';$('#controlList').innerHTML=addBtn+(a.map((x,i)=>`<button class="row" data-ci="${i}"><span>${x[0]}</span><span class="main"><b>${esc(x[1])}</b><small>${esc(x[2])}</small></span>${pill(x[3])}</button>`).join('')||empty());if($('#addHandover'))$('#addHandover').onclick=handoverForm;$$('[data-ci]').forEach((b,i)=>b.onclick=()=>toggleItem(i))}
$$('[data-tab]').forEach(b=>b.onclick=()=>{$$('[data-tab]').forEach(x=>x.classList.toggle('on',x===b));tab=b.dataset.tab;renderControl()});function toggleItem(i){let map={decision:'decisions',variation:'variations',inspect:'inspections',handover:'handover'},k=map[tab];if(!k)return;let x=s[k][i];if(tab==='decision')x.status=x.status==='已确认'?'待确认':'已确认';if(tab==='variation')x.status=x.status==='已批准'?'待批准':'已批准';if(tab==='inspect')x.status=x.status==='通过'?'待检查':'通过';if(tab==='handover')x.status=x.status==='已收齐'?'待收齐':'已收齐';save();render()}
function renderBudget(){let t=totals();$('#remain').textContent=rm(s.budget-t.final);$('#bbudget').textContent=rm(s.budget);$('#bcommit').textContent=rm(t.commit);$('#bpaid').textContent=rm(t.actual);$('#bfinal').textContent=rm(t.final);$('#moneyNote').textContent=`已承诺 ${rm(t.commit)} · 已支付 ${rm(t.actual)} · 其中选购已实际发生 ${rm(t.shopActual)}，待购买已选商品 ${rm(t.shopForecast)}。`;let bb=$('#budgetBuckets');if(bb)bb.innerHTML=(s.budgetBuckets||[]).map(x=>`<div class="bucket"><small>${esc(x.name)}</small><b>${rm(x.spent||0)} / ${rm(x.budget||0)}</b><em>${esc(x.note||'')}</em></div>`).join('');let a=btab==='quotes'?s.quotes.map(x=>['🧾',x.title||x.vendor||'报价',rm(x.total||x.quote),(x.status||'已选')+(x.vendor?' · '+x.vendor:'')]):btab==='payments'?s.payments.map(x=>['💳',x.title||'付款',rm(x.amount),x.date||'']):s.expenses.map(x=>['💰',x.name,rm(x.amount),x.date||'']);$('#budgetList').innerHTML=a.map(x=>`<div class="row"><span>${x[0]}</span><span class="main"><b>${esc(x[1])}</b><small>${esc(x[3])}</small></span><span class="amount">${x[2]}</span></div>`).join('')||empty()}$$('[data-btab]').forEach(b=>b.onclick=()=>{$$('[data-btab]').forEach(x=>x.classList.toggle('on',x===b));btab=b.dataset.btab;renderBudget()});
function modal(t,sub,html){$('#mt').textContent=t;$('#ms').textContent=sub||'';$('#mb').innerHTML=html;$('#modal').classList.remove('hide')}$('#mc').onclick=()=>$('#modal').classList.add('hide');function form(t,html,fn){modal(t,'',html+'<button class="primary" id="fs">保存</button><button class="secondary" id="fc">取消</button>');$('#fc').onclick=()=>$('#modal').classList.add('hide');$('#fs').onclick=fn}
function setup(){let o=s||blank();form('建立 / 编辑装修项目',`<label>项目名称<input id="n" value="${esc(o.name)}"></label><div class="two"><label>房屋类型<select id="ty"><option>Condo</option><option>Terrace</option><option>Semi-D</option><option>Bungalow</option><option>Apartment</option></select></label><label>地区<input id="ar" value="${esc(o.area)}"></label></div><label>空间（逗号分隔）<input id="rs" value="${esc((o.rooms.length?o.rooms:['客厅','主人房','厨房','浴室']).join(','))}"></label><label>阶段<select id="st">${ST.map(x=>`<option ${o.stage===x?'selected':''}>${x}</option>`).join('')}</select></label><label>预算 RM<input id="bu" inputmode="decimal" value="${o.budget||''}"></label>`,()=>{let n=$('#n').value.trim();if(!n)return;s=norm({...o,name:n,type:$('#ty').value,area:$('#ar').value.trim(),rooms:$('#rs').value.split(',').map(x=>x.trim()).filter(Boolean),stage:$('#st').value,budget:Number($('#bu').value)||0});s.rooms.forEach(r=>s.roomData[r]=s.roomData[r]||{progress:0});save();$('#modal').classList.add('hide');render()})}
function roomHub(r,rtab='overview'){let d=s.roomData[r]||{},tasks=s.tasks.filter(x=>x.room===r),decs=s.decisions.filter(x=>x.room===r),defs=s.defects.filter(x=>x.room===r),shops=s.shopping.filter(x=>x.room===r),logs=s.siteLogs.filter(x=>x.room===r),ins=s.inspections.filter(x=>x.room===r),socks=s.sockets.filter(x=>x.room===r);let body='';if(rtab==='overview'){body=`<div class="roomMetrics"><div><small>房间进度</small><b>${d.progress||0}%</b></div><div><small>待办</small><b>${tasks.filter(x=>!x.done).length}</b></div><div><small>待确认</small><b>${decs.filter(x=>x.status!=='已确认').length}</b></div><div><small>Defect</small><b>${defs.filter(x=>x.status!=='已解决').length}</b></div></div><div class="card"><div class="kv"><span>尺寸 / 重点</span><b>${esc(d.dimensions||'未记录')}</b></div><div class="kv"><span>设计方向</span><b>${esc(d.design||'未记录')}</b></div><div class="kv"><span>现场备注</span><b>${esc(d.note||'未记录')}</b></div></div><div class="roomHubActions"><button id="roomEdit">编辑资料</button><button id="roomTaskAdd">＋任务</button><button id="roomShopAdd">＋商品</button></div>`}if(rtab==='actions'){bo