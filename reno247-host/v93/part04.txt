dy=`<div class="head"><h3>决定与待办</h3><button id="roomDecisionAdd">＋待确认</button></div>${[...decs.map(x=>['◇',x.title,x.due?('截止 '+x.due):(x.note||''),x.status||'待确认']),...tasks.map(x=>['✅',x.name,x.due?('截止 '+x.due):(x.note||''),x.done?'已完成':'待处理']),...defs.map(x=>['⚠️',x.title,x.owner||x.note||'',x.status||'待处理'])].map(x=>`<div class="row"><span>${x[0]}</span><span class="main"><b>${esc(x[1])}</b><small>${esc(x[2])}</small></span>${pill(x[3])}</div>`).join('')||empty('这个房间目前没有事项')}`}if(rtab==='shopping'){body=`<div class="head"><h3>这个房间的选购</h3><button id="roomShopAdd2">＋收藏</button></div><div id="roomShopList">加载中…</div>`}if(rtab==='site'){body=`<div class="head"><h3>施工记录</h3><button id="roomLogAdd">＋日志</button></div>${logs.map(x=>`<div class="row"><span>📝</span><span class="main"><b>${esc(x.date||'现场日志')}</b><small>${esc(x.work||x.note||'')}</small></span></div>`).join('')||empty('还没有这个房间的施工日志')}`}if(rtab==='inspect'){body=`<div class="head"><h3>验收</h3><button id="roomInspectAdd">＋验收</button></div>${ins.map(x=>`<div class="row"><span>✓</span><span class="main"><b>${esc(x.title)}</b><small>${esc(x.due?('计划 '+x.due):(x.note||''))}</small></span>${pill(x.status||'待检查')}</div>`).join('')||empty('还没有这个房间的验收记录')}`};if(rtab==='electrical'){body=`<div class="head"><h3>电位 / 网络点</h3></div>${socks.map(x=>`<div class="row"><span>🔌</span><span class="main"><b>${esc(x.title)}</b><small>${esc([x.position,x.qty,x.type,x.note].filter(Boolean).join(' · '))}</small></span>${pill(x.status||'待确认')}</div>`).join('')||empty('这个房间还没有电位记录')}`};modal(r,'Room Digital Twin · 所有资料都围绕房间归档',`<div class="roomHubTop"><div class="roomHubIcon">${EM[r]||'🏠'}</div><div><b>${esc(r)}</b><small>${d.progress||0}% · ${tasks.filter(x=>!x.done).length} 待办 · ${shops.length} 选购 · ${socks.length} 电位 · ${defs.filter(x=>x.status!=='已解决').length} Defect</small><div class="bar"><i style="width:${d.progress||0}%"></i></div></div></div><div class="tabs">${[['overview','总览'],['actions','事项'],['shopping','选购'],['electrical','电位'],['site','现场'],['inspect','验收']].map(x=>`<button class="${rtab===x[0]?'on':''}" data-rtab="${x[0]}">${x[1]}</button>`).join('')}</div>${body}`);$$('[data-rtab]').forEach(b=>b.onclick=()=>roomHub(r,b.dataset.rtab));if($('#roomEdit'))$('#roomEdit').onclick=()=>roomEdit(r);if($('#roomTaskAdd'))$('#roomTaskAdd').onclick=()=>add('task',r);if($('#roomShopAdd'))$('#roomShopAdd').onclick=()=>productForm(null,r);if($('#roomDecisionAdd'))$('#roomDecisionAdd').onclick=()=>add('decision',r);if($('#roomShopAdd2'))$('#roomShopAdd2').onclick=()=>productForm(null,r);if($('#roomLogAdd'))$('#roomLogAdd').onclick=()=>add('log',r);if($('#roomInspectAdd'))$('#roomInspectAdd').onclick=()=>add('inspect',r);if(rtab==='shopping')productRows(shops).then(h=>{let e=$('#roomShopList');if(e){e.innerHTML=h||empty('还没有收藏商品');$$('[data-prod]').forEach(b=>b.onclick=()=>productDetail(b.dataset.prod))}})}function roomEdit(r){let d=s.roomData[r]||{};form('编辑 '+r,`<label>进度 %<input id="rp" type="number" min="0" max="100" value="${d.progress||0}"></label><label>尺寸 / 重点<input id="rd" value="${esc(d.dimensions||'')}"></label><label>设计方向<textarea id="rde">${esc(d.design||'')}</textarea></label><label>现场备注<textarea id="rn">${esc(d.note||'')}</textarea></label>`,()=>{s.roomData[r]={progress:Math.min(100,Number($('#rp').value)||0),dimensions:$('#rd').value.trim(),design:$('#rde').value.trim(),note:$('#rn').value.trim()};save();render();roomHub(r)})}
function handoverForm(){form('交付 / Warranty',`<label>项目<input id="ht" placeholder="例如：智能门锁 Warranty"></label><label>供应商 / 负责人<input id="hv"></label><div class="two"><label>交付截止<input id="hd" type="date"></label><label>保固到期<input id="he" type="date"></label></div><label>备注<textarea id="hnote"></textarea></label>`,()=>{let title=$('#ht').value.trim();if(!title)return;s.handover.unshift({id:uid(),title,vendor:$('#hv').value.trim(),due:$('#hd').value,expiry:$('#he').value,note:$('#hnote').value.trim(),status:'待收齐'});save();render();tab='handover';$('#modal').classList.add('hide');go('control');renderControl()})}
function add(kind,prefRoom=''){if(kind==='shopping')return productForm(null,prefRoom);let cfg={task:['新增任务','任务名称'],decision:['新增待确认','需要确认什么'],variation:['新增变更单','变更内容'],log:['施工日志','今天做了什么'],inspect:['新增验收','验收项目']}[kind];let extra='';if(kind==='variation')extra='<label>金额 RM<input id="amt" inputmode="decimal"></label>';if(['task','decision','inspect'].includes(kind))extra+=`<label>${kind==='decision'?'最迟确认日期':'截止 / 计划日期'}<input id="due" type="date"></label>`;if(kind==='decision')extra+='<label>如果不确认，会阻塞什么？<input id="block" placeholder="例如：橱柜下单 / 水电定位"></label>';form(cfg[0],`<label>${cfg[1]}<input id="val"></label><label>空间<select id="room"><option value="">未指定</option>${s.rooms.map(r=>`<option ${prefRoom===r?'selected':''}>${esc(r)}</option>`).join('')}</select></label>${extra}<label>备注<textarea id="note"></textarea></label>`,()=>{let v=$('#val').value.trim();if(!v)return;let room=$('#room').value,note=$('#note').value.trim(),x={id:uid(),room,note};if(kind==='task'){x.name=v;x.done=false;x.due=$('#due').value;s.tasks.unshift(x)}if(kind==='decision'){x.title=v;x.status='待确认';x.due=$('#due').value;x.block=$('#block').value.trim();s.decisions.unshift(x)}if(kind==='variation'){x.title=v;x.amount=Number($('#amt').value)||0;x.status='待批准';s.variations.unshift(x)}if(kind==='log'){x.work=v;x.date=day();s.siteLogs.unshift(x)}if(kind==='inspect'){x.title=v;x.status='待检查';x.due=$('#due').value;s.inspections.unshift(x)}save();render();$('#modal').classList.add('hide')})}
function openShopping(){$('#modal').classList.add('hide');tab='shopping';$$('[data-tab]').forEach(b=>b.classList.toggle('on',b.dataset.tab==='shopping'));go('control');renderControl()}
$('#shopOpen').onclick=openShopping;
function idb(){return new Promise((ok,bad)=>{let r=indexedDB.open('reno247_v91_media',1);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains('files'))r.result.createObjectStore('files')};r.onsuccess=()=>ok(r.result);r.onerror=()=>bad(r.error)})}
async function mediaPut(file){let d=await idb(),k='shop_'+uid();await new Promise((ok,bad)=>{let tx=d.transaction('files','readwrite');tx.objectStore('files').put(file,k);tx.oncomplete=ok;tx.onerror=()=>bad(tx.error)});return k}
async function mediaGet(k){if(!k)return'';try{let d=await idb(),b=await new Promise(ok=>{let r=d.transaction('files').objectStore('files').get(k);r.onsuccess=()=>ok(r.result);r.onerror=()=>ok(null)});return b?URL.createObjectURL(b):''}catch(e){return''}}
function pickShopImage(){return new Promise(ok=>{let f=$('#shopFile');f.value='';f.onchange=async()=>ok(f.files&&f.files[0]?await mediaPut(f.files[0]):'');f.click()})}
function statusOptions(cur){return SHOP_STATUS.map(x=>`<option ${cur===x?'selected':''}>${x}</option>`).join('')}
function safeOpen(u){if(/^https?:\/\//i.test(String(u||'')))window.open(u,'_blank','noopener')}
async function productForm(existing=null,prefRoom=''){let x=existing||{id:uid(),item:'',room:prefRoom,category:'',platform:'',url:'',price:0,shipping:0,qty:1,target:0,actual:0,group:'',status:'想买',note:'',imgKey:''},imgKey=x.imgKey||'',img=await mediaGet(imgKey);modal(existing?'编辑商品':'收藏商品','把网上看到的商品先放进 Reno247',`<div class="imgPick"><div id="shopPreview" class="preview">${img?`<img src="${img}" style="width:100%;height:100%;object-fit:cover;border-radius:14px">`:'商品截图'}</div><button class="secondary" id="pickShopImg" type="button">${imgKey?'更换截图':'添加截图'}</button></div><label>商品名称<input id="si" value="${esc(x.item)}" placeholder="例如：餐厅吊灯"></label><div class="two"><label>房间<select id="sr"><option value="">未指定</option>${s.rooms.map(r=>`<option ${x.room===r?'selected':''}>${esc(r)}</option>`).join('')}</select></label><label>分类<input id="sc" value="${esc(x.category)}" placeholder="灯具 / 家电 / 家具"></label></div><div class="two"><label>来源<select id="sp"><option></option>${['Shopee','Lazada','淘宝','IKEA','小红书','门店','其他'].map(v=>`<option ${x.platform===v?'selected':''}>${v}</option>`).join('')}</select></label><label>状态<select id="ss">${statusOptions(x.status)}</select></label></div><label>商品链接<input id="su" value="${esc(x.url)}" placeholder="https://..."></label><div class="two"><label>商品价 RM<input id="sprice" inputmode="decimal" value="${x.price||''}"></label><label>运费 RM<input id="sship" inputmode="decimal" value="${x.shipping||''}"></label></div><div class="two"><label>数量<input id="sqty" inputmode="numeric" value="${x.qty||1}"></label><label>心理价 RM<input id="starget" inputmode="decimal" value="${x.target||''}"></label></div><div class="two"><label>实际商品价 RM<input id="sactual" inputmode="decimal" value="${x.actual||''}"></label><label>对比组<input id="sgroup" value="${esc(x.group)}" placeholder="例如：餐厅吊灯"></label></div><label>预计到货 / 想购买日期<input id="sexpected" type="date" value="${esc(x.expected||'')}"></label><label>备注<textarea id="snote">${esc(x.note)}</textarea></label><p class="shopHint">候选/比价中的商品不会计入预算；已选/等优惠会进入预计采购；已下单以后会进入实际采购。</p><button class="primary" id="saveProduct">保存商品</button>${existing?'<button class="danger" id="deleteProduct">删除商品</button>':''}`);$('#pickShopImg').onclick=async()=>{let k=await pickShopImage();if(k){imgKey=k;let u=await mediaGet(k);$('#shopPreview').innerHTML=`<img src="${u}" style="width:100%;height:100%;object-fit:cover;border-radius:14px">`}};$('#saveProduct').onclick=()=>{let name=$('#si').value.trim();if(!name)return $('#si').focus();Object.assign(x,{item:name,room:$('#sr').value,category:$('#sc').value.trim(),platform:$('#sp').value,url:$('#su').value.trim(),price:Number($('#sprice').value)||0,shipping:Number($('#sship').value)||0,qty:Number($('#sqty').value)||1,target:Number($('#starget').value)||0,actual:Number($('#sactual').value)||0,group:$('#sgroup').value.trim(),status:$('#ss').value,expected:$('#sexpected').value,note:$('#snote').value.trim(),imgKey});if(!existing)s.shopping.unshift(x);save();render();openShopping()};if(existing)$('#deleteProduct').onclick=()=>{if(confirm('删除这个商品？')){s.shopping=s.shopping.filter(v=>v.id!==x.id);save();render();openShopping()}}}
async function productRows(items){let out='';for(let x of items){let img=await mediaGet(x.imgKey),target=x.target&&landed(x)>x.target?` · 目标 ${rm(x.target)}`:'';out+=`<button class="product" data-prod="${x.id}">${img?`<img src="${img}">`:'<div class="prodph">🛒</div>'}<span class="main"><b>${esc(x.item)}</b><small>${esc(x.platform||'未注明来源')} · ${esc(x.room||'未分房间')}${target}</small><span class="statusline">${pill(x.status)}${x.group?`<span class="pill">${esc(x.group)}</span>`:''}</span></span><span class="price"><b>${rm(landed(x))}</b><small>到手价</small></span></button>`}return out}
async function renderShoppingHub(){let sm=shopMoney(),cand=s.shopping.filter(x=>['想买','找资料','比价中'].includes(x.status)).length,sel=s.shopping.filter(x=>['已选','等优惠'].includes(x.status)).length,bought=s.shopping.filter(x=>['已下单','运输中','已到货','已安装','完成'].includes(x.status)).length;let filtered=s.shopping;if(shopView==='candidate')filtered=filtered.filter(x=>['想买','找资料','比价中'].includes(x.status));if(shopView==='selected')filtered=filtered.filter(x=>['已选','等优惠'].includes(x.status));if(shopView==='ordered')filtered=filtered.filter(x=>['已下单','运输中','已到货','已安装','完成'].includes(x.status));let body='';if(shopView==='compare'){let gs={};s.shopping.filter(x=>x.group).forEach(x=>(gs[x.group]??=[]).push(x));let groups=Object.entries(gs).filter(([k,v])=>v.length>1);body=groups.map(([g,v])=>`<button class="compareGroup" data-group="${esc(g)}"><b>${esc(g)}</b><small>${v.length} 个候选 · ${rm(Math.min(...v.map(landed)))} – ${rm(Math.max(...v.map(landed)))}</small></button>`).join('')||empty('还没有对比组。给多个商品填写相同的“对比组”即可。')}else body=await productRows(filtered);$('#controlList').innerHTML=`<div class="shopStats"><div><small>候选</small><b>${cand}</b></div><div><small>已选</small><b>${sel}</b></div><div><small>已购买</small><b>${bought}</b></div><div><small>预计采购</small><b>${rm(sm.forecast)}</b></div></div><div class="shopToolbar"><button id="addProduct">＋ 收藏商品</button><button id="compareProduct