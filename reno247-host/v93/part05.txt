s">⚖️ 对比商品</button></div><div class="tabs"><button data-shopview="all" class="${shopView==='all'?'on':''}">全部</button><button data-shopview="candidate" class="${shopView==='candidate'?'on':''}">候选</button><button data-shopview="selected" class="${shopView==='selected'?'on':''}">已选</button><button data-shopview="ordered" class="${shopView==='ordered'?'on':''}">购买中</button><button data-shopview="compare" class="${shopView==='compare'?'on':''}">对比组</button></div><div id="shopItems">${body||empty('还没有收藏商品')}</div>`;$('#addProduct').onclick=()=>productForm();$('#compareProducts').onclick=()=>{shopView='compare';renderShoppingHub()};$$('[data-shopview]').forEach(b=>b.onclick=()=>{shopView=b.dataset.shopview;renderShoppingHub()});$$('[data-prod]').forEach(b=>b.onclick=()=>productDetail(b.dataset.prod));$$('[data-group]').forEach(b=>b.onclick=()=>compareDetail(b.dataset.group))}
async function productDetail(id){let x=s.shopping.find(v=>v.id===id);if(!x)return;let img=await mediaGet(x.imgKey),lc=landed(x);modal(x.item,`${x.platform||'商品'} · ${x.room||'未分房间'}`,`${img?`<img src="${img}" style="width:100%;max-height:260px;object-fit:cover;border-radius:16px;margin-bottom:10px">`:''}<div class="card"><div class="kv"><span>状态</span><b>${esc(x.status)}</b></div><div class="kv"><span>商品价</span><b>${rm(x.price)}</b></div><div class="kv"><span>运费</span><b>${rm(x.shipping)}</b></div><div class="kv"><span>到手价</span><b>${rm(lc)}</b></div><div class="kv"><span>心理价</span><b>${x.target?rm(x.target):'未设'}</b></div><div class="kv"><span>对比组</span><b>${esc(x.group||'未分组')}</b></div><div class="kv"><span>预计日期</span><b>${esc(x.expected||'未设')}</b></div></div>${x.url?'<button class="secondary" id="openProduct">打开商品链接</button>':''}<button class="primary" id="editProduct">编辑商品</button>`);if(x.url)$('#openProduct').onclick=()=>safeOpen(x.url);$('#editProduct').onclick=()=>productForm(x)}
async function compareDetail(group){let items=s.shopping.filter(x=>x.group===group),rows='';for(let x of items){rows+=`<div class="row"><span>🛒</span><span class="main"><b>${esc(x.item)}</b><small>${esc(x.platform||'')} · ${esc(x.status)}</small></span><span class="amount">${rm(landed(x))}</span><button class="linkbtn" data-choose="${x.id}">选择</button></div>`}modal('商品对比',group,`<p class="shopHint">选择后，该商品会进入“已选”，同一对比组内其他候选会保留并标记为“未选择”。</p>${rows}`);$$('[data-choose]').forEach(b=>b.onclick=()=>{let chosen=s.shopping.find(x=>x.id===b.dataset.choose);for(let x of s.shopping.filter(x=>x.group===group)){if(['想买','找资料','比价中','已选','等优惠','未选择'].includes(x.status))x.status=x.id===chosen.id?'已选':'未选择'}save();render();openShopping()})}

$('#fab').onclick=()=>$('#quick').classList.remove('hide');$('#qc').onclick=()=>$('#quick').classList.add('hide');$$('[data-q]').forEach(b=>b.onclick=()=>{$('#quick').classList.add('hide');add(b.dataset.q)});$('#addRoom').onclick=()=>{let r=prompt('空间名称');if(r&&(r=r.trim())&&!s.rooms.includes(r)){s.rooms.push(r);s.roomData[r]={progress:0};save();render()}};$('#edit').onclick=setup;if($('#notionReport'))$('#notionReport').onclick=()=>{let n=s.notionImport||{};modal('Notion 迁移报告','Notion → Reno247 Import V1',`<div class="card"><div class="kv"><span>Shopping Hub</span><b>${n.shoppingImported||0} 条</b></div><div class="kv"><span>房间</span><b>${n.rooms||0}</b></div><div class="kv"><span>支出</span><b>${n.expenses||0}</b></div><div class="kv"><span>师傅</span><b>${n.contractors||0}</b></div><div class="kv"><span>电位</span><b>${n.sockets||0}</b></div><div class="kv"><span>Defect</span><b>${n.defects||0}</b></div><div class="kv"><span>待确认/决定</span><b>${n.decisions||0}</b></div><div class="kv"><span>验收</span><b>${n.inspections||0}</b></div><div class="kv"><span>任务</span><b>${n.tasks||0}</b></div><div class="kv"><span>Notion 查询限制待补</span><b>${n.pendingShopping||0} 条采购</b></div></div>`)};$('#export').onclick=()=>{let b=new Blob([JSON.stringify(s,null,2)],{type:'application/json'}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download='reno247-v9-backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(u),500)};$('#reset').onclick=()=>{if(confirm('清除 Reno247 V9 本机资料？')){localStorage.removeItem(K);location.reload()}};$('#bell').onclick=()=>alert('目前没有新的装修提醒。');if(s)render();else setup();
</script>
<style id="v922-style">
/* V9.2.2 — content-aware information architecture */
.topActions{display:flex;gap:8px;align-items:center}.searchCircle{font-size:0}.searchCircle:before{content:"⌕";font-size:24px;font-weight:500;line-height:1}
.contentPulse{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-top:14px}.pulseCard{border:1px solid var(--line);background:#fff;border-radius:17px;padding:13px;text-align:left;box-shadow:0 6px 18px rgba(25,29,38,.03)}.pulseCard small{display:block;color:var(--muted);font-size:10px}.pulseCard b{display:block;font-size:19px;margin-top:4px}.pulseCard em{display:block;font-style:normal;font-size:9px;color:var(--muted);margin-top:4px}.pulseCard.attn{background:#fffafa;border-color:#f2d7d9}.pulseCard.attn b{color:var(--accent)}
.floorGroup{margin-bottom:22px}.floorHead{display:flex;justify-content:space-between;align-items:end;margin:2px 2px 9px}.floorHead b{font-size:15px}.floorHead small{color:var(--muted);font-size:10px}.roomArea{color:#555!important;font-weight:700}.roomMetaLine{display:flex;gap:7px;flex-wrap:wrap;margin-top:4px}.roomMetaLine span{font-size:9px;color:#7d8490;background:#f4f5f7;border-radius:99px;padding:4px 7px}.roomMetaLine span.warn{background:#fff0f2;color:#c83f4a}.roomMetaLine span.ok{background:#eaf7f0;color:#2b8b5c}
.controlGrid{display:grid;grid-template-columns:1fr 1fr;gap:9px}.controlTile{border:1px solid var(--line);background:#fff;border-radius:18px;padding:14px;text-align:left;box-shadow:0 6px 18px rgba(25,29,38,.03)}.controlTile .ico{width:36px;height:36px;border-radius:12px;background:#f3f4f7;display:grid;place-items:center;font-size:17px}.controlTile b{display:block;font-size:20px;margin-top:10px}.controlTile span{display:block;font-size:11px;font-weight:750;margin-top:2px}.controlTile small{display:block;font-size:9px;color:var(--muted);margin-top:4px}.controlTile.attn{border-color:#f1d7da;background:#fffafa}.controlTile.attn b{color:var(--accent)}.blockerList{margin-top:14px}.blockerList .row{margin-bottom:7px}
.shopSearch{display:flex;gap:8px;margin:11px 0}.shopSearch input{margin:0;min-height:44px}.shopSearch button{flex:none;width:44px;border:0;border-radius:13px;background:#f0f1f4;font-size:18px}.phaseRail{display:flex;gap:6px;overflow:auto;scrollbar-width:none;padding:1px 0 7px}.phaseRail::-webkit-scrollbar{display:none}.phaseRail button{border:1px solid #e4e6eb;background:#fff;border-radius:99px;padding:8px 10px;font-size:10px;font-weight:750;white-space:nowrap}.phaseRail button.on{background:#171a20;color:#fff;border-color:#171a20}.shopGroupTitle{display:flex;justify-content:space-between;align-items:center;margin:17px 2px 8px}.shopGroupTitle b{font-size:13px}.shopGroupTitle small{font-size:10px;color:var(--muted)}.priceMissing{color:#a96b18!important}.shopMore{width:100%;border:1px dashed #d5d8df;background:#fff;border-radius:15px;padding:12px;font-weight:750;color:#666;margin:6px 0 14px}.dataHint{border-radius:16px;background:#fff8ea;border:1px solid #f3e3bd;padding:12px 13px;color:#77551c;font-size:10px;line-height:1.45;margin:9px 0 12px}.dataHint b{font-size:11px}.shopStats.compact{grid-template-columns:repeat(2,1fr)}
.bucket{position:relative;overflow:hidden}.bucketUsage{height:5px;background:#eceef2;border-radius:99px;overflow:hidden;margin-top:9px}.bucketUsage i{display:block;height:100%;border-radius:99px;background:linear-gradient(90deg,#ff6a77,#ff3d55)}.bucket strong{font-size:10px;position:absolute;right:12px;top:12px;color:#737985}.bucket strong.warn{color:#c45530}.budgetLead{background:#fff;border:1px solid var(--line);border-radius:18px;padding:14px;margin:12px 0;display:flex;justify-content:space-between;gap:12px;align-items:center}.budgetLead small{display:block;color:var(--muted);font-size:10px}.budgetLead b{display:block;font-size:18px;margin-top:2px}.budgetLead .right{text-align:right}
.searchResults{display:grid;gap:8px}.searchGroup{margin-top:14px}.searchGroup>small{display:block;color:var(--muted);font-size:10px;font-weight:750;margin:0 4px 7px}.searchItem{width:100%;border:1px solid var(--line);background:#fff;border-radius:15px;padding:12px;display:flex;gap:10px;align-items:center;text-align:left}.searchItem .sico{width:34px;height:34px;border-radius:11px;background:#f3f4f7;display:grid;place-items:center;flex:none}.searchItem .scopy{flex:1;min-width:0}.searchItem b{display:block;font-size:12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.searchItem small{display:block;color:var(--muted);font-size:9px;margin-top:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.searchItem>span:last-child{color:#aeb3bd}
.stageReadyLabel{font-size:19px!important}.navBadge{position:absolute;top:1px;right:18%;min-width:16px;height:16px;border-radius:99px;background:var(--accent);color:#fff;display:grid;place-items:center;font-size:8px;font-weight:800;padding:0 4px}.nav button{position:relative}
@media(max-width:370px){.controlGrid,.contentPulse{grid-template-columns:1fr}.navBadge{right:12%}}
</style>

<script id="v922-script">
(()=>{
  const V='V9.2.2';
  const GF=['客厅','餐厅','厨房','电竞/工作房','客用浴室','Yard','Terrace','楼梯','车Porch'];
  const FF=['主人房','房2','房3','主浴室','浴室3','Family Area','阳台'];
  const PHASES=['全部','3个月前','1个月前','入住时','锦上添花','待定'];
  window.__reno922={shopPhase:'全部',shopQuery:'',shopLimit:30};
  const R=window.__reno922;
  function phaseOf(x){const n=x?.note||'';if(n.includes('3个月前'))return'3个月前';if(n.includes('1个月前'))return'1个月前';if(n.includes('入住时'))return'入住时';if(n.includes('锦上添花'))return'锦上添花';if(n.includes('待定'))return'待定';return'未分类'}
  function missingPrice(x){return Number(x?.price||0)<=0 && Number(x?.actual||0)<=0}
  function priceLabel(x){return missingPrice(x)?'待补价格':rm(landed(x))}
  function fullOpenCount(k){if(k==='task')return s.tasks.filter(x=>!x.done).length;if(k==='decision')return s.decisions.filter(x=>x.status!=='已确认').length;if(k==='defect')return s.defects.filter(x=>x.status!=='已解决').length;if(k==='inspect')return s.inspections.filter(x=>x.status!=='通过').length;return 0}

  // Diversify Next Action: prevent Defects from swallowing the entire home feed.
  nextActions=function(){
    const pool=[];
    for(const x of s.tasks.filter(x=>!x.done)){const dd=dateDelta(x.due);pool.push({kind:'task',icon:'✅',title:x.name,sub:x.room||x.note||'装修待办',status:dd<0?'已逾期':dd===0?'今天':'待处理',score:dd<0?100:dd===0?95:dd<=2?85:70,due:x.due||''})}
    for(const x of s.decisions.filter(x=>x.status!=='已确认')){const dd=dateDelta(x.due);pool.push({kind:'decision',icon:'◇',title:x.title,sub:[x.room,x.block?'会阻塞：'+x.block:''].filter(Boolean).join(' · ')||'需要确认',status:dd<0?'已逾期':x.block?'会阻塞施工':'待确认',score:x.block?96:(dd<=2?90:78),due:x.due||''})}
    for(const x of s.defects.filter(x=>x.status!=='已解决'))pool.push({kind:'defect',icon:'⚠️',title:x.title||'处理 Defect',sub:x.room||x.owner||'待复验',status:x.status||'待复验',score:89,due:x.due||''});
    for(const x of s.inspections.filter(x=>x.status!=='通过')){const dd=dateDelta(x.due);pool.push({kind:'inspect',icon:'✓',title:x.title,sub:x.room||x.note||'阶段验收',status:dd<0?'验收逾期':'待验收',score:dd<0?92:80,due:x.due||''})}
    for(const x of s.variations.filter(x=>x.status==='待批准'))pool.push({kind:'variation',icon:'±',title:x.title,sub:(x.room?x.room+' · ':'')+rm(x.amount),status:'待批准',score:84,due:x.due||''});
    for(const x of s.shopping.filter(x=>['已选','等优惠'].includes(x.status)))pool.push({kind:'shop',icon:'🛒',title:'待购买：'+x.item,sub:[x.room,phaseOf(x)].filter(Boolean).join(' · '),status:x.status,score:62,due:x.expected||''});
    pool.sort((a,b)=>b.score-a.score||dateDelta(a.due)-dateDelta(b.due));
    const caps={defect:2,decision:2,task:2,inspect:1,variation:1,shop:1},used={},out=[];
    for(const x of pool){if((used[x.kind]||0)>=(caps[x.kind]||1))co