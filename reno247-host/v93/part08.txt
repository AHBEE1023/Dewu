nd normalize every historical module instead of discarding old fields.
  const extraArrays=['ideas','roomPhotos','markers','schedule','paymentReminders'];
  for(const k of extraArrays) if(!Array.isArray(s[k])) s[k]=[];
  if(!('plan' in s)) s.plan=null;
  s.siteLogs=s.siteLogs.map(x=>({...x,photos:Array.isArray(x.photos)?x.photos:[]}));
  s.defects=s.defects.map(x=>({...x,before:x.before||'',after:x.after||''}));
  s.decisions=s.decisions.map(x=>({...x,kind:x.kind||'decision',priority:x.priority||'普通',options:Array.isArray(x.options)?x.options:[],chosenOption:x.chosenOption||''}));
  s.quotes=s.quotes.map(x=>({...x,status:x.status||'已选',compareGroup:x.compareGroup||x.group||[x.room,x.title].filter(Boolean).join(' · ')||'未分组报价'}));
  s.schedule=s.schedule.map(x=>({...x,status:x.status||'计划'}));
  s.paymentReminders=s.paymentReminders.map(x=>({...x,status:x.status||'待付款'}));
  const oldSave=save; save=function(){s.v=9.3;localStorage.setItem(K,JSON.stringify(s))}; save();

  // Image compatibility: new V9.1 media DB + legacy V7/V8/V9 media DB.
  let legacyDbP=null;
  function legacyDb(){return legacyDbP||(legacyDbP=new Promise((ok,bad)=>{let r=indexedDB.open('reno247_media',1);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains('f'))r.result.createObjectStore('f')};r.onsuccess=()=>ok(r.result);r.onerror=()=>bad(r.error)}))}
  async function legacyGet(k){if(!k)return'';try{let d=await legacyDb(),b=await new Promise(ok=>{let r=d.transaction('f').objectStore('f').get(k);r.onsuccess=()=>ok(r.result);r.onerror=()=>ok(null)});return b?URL.createObjectURL(b):''}catch(e){return''}}
  async function anyMedia(k){if(!k)return'';let u='';try{u=await mediaGet(k)}catch(e){}return u||await legacyGet(k)}
  async function pickImage(){return new Promise(ok=>{let f=document.createElement('input');f.type='file';f.accept='image/*';f.style.display='none';document.body.appendChild(f);f.onchange=async()=>{let k='';if(f.files?.[0])try{k=await mediaPut(f.files[0])}catch(e){}f.remove();ok(k)};f.click()})}

  function roomOpts(cur=''){return `<option value="">未指定</option>${s.rooms.map(r=>`<option ${cur===r?'selected':''}>${esc(r)}</option>`).join('')}`}
  function normalizeStatus(v,arr,def){return arr.includes(v)?v:def}

  // ---------- FLOOR PLAN ----------
  async function floorPlanView(){
    if(!s.plan){modal('Floor Plan','上传平面图后直接在图上标插座、灯位、柜体、尺寸、水位和网络点。',`<button class="primary" id="uploadPlanFull">📐 上传 Floor Plan 图片</button><div class="restoreNotice">Building Plan 的文字尺寸资料仍保留在各房间；这里是可视化点位层，不会取代房间资料。</div>`);$('#uploadPlanFull').onclick=async()=>{let k=await pickImage();if(k){s.plan=k;save();floorPlanView()}};return}
    const url=await anyMedia(s.plan);if(!url){modal('Floor Plan','旧图片读取不到，但标记资料仍然保留。',`<button class="primary" id="replacePlanFull">重新上传 Floor Plan</button>`);$('#replacePlanFull').onclick=async()=>{let k=await pickImage();if(k){s.plan=k;save();floorPlanView()}};return}
    modal('Floor Plan','开启标记模式后直接点击图纸位置。',`<div class="planWrap" id="planWrap"><img id="planImageFull" src="${url}"></div><div class="two"><button class="secondary" id="toggleMarkFull">📍 开始标记</button><button class="secondary" id="replacePlanFull">更换图片</button></div><section class="section"><div class="head"><h3>点位标记</h3><button id="addMarkerList">＋手动新增</button></div><div id="markerListFull"></div></section>`);
    let active=false,img=$('#planImageFull'),wrap=$('#planWrap');
    const redraw=()=>{wrap.querySelectorAll('.markerDot').forEach(x=>x.remove());s.markers.forEach((m,i)=>{let b=document.createElement('button');b.className='markerDot';b.style.left=(Number(m.x)||0)*100+'%';b.style.top=(Number(m.y)||0)*100+'%';b.textContent=i+1;b.onclick=e=>{e.stopPropagation();markerFormFull(m)};wrap.appendChild(b)});$('#markerListFull').innerHTML=s.markers.map((m,i)=>`<button class="row" data-markerfull="${m.id}"><span>${i+1}</span><span class="main"><b>${esc(m.label||'未命名点位')}</b><small>${esc([m.room,m.type,m.note].filter(Boolean).join(' · '))}</small></span></button>`).join('')||empty('还没有标记');$$('[data-markerfull]').forEach(b=>b.onclick=()=>markerFormFull(s.markers.find(x=>x.id===b.dataset.markerfull)))};redraw();
    $('#toggleMarkFull').onclick=()=>{active=!active;$('#toggleMarkFull').textContent=active?'✓ 完成标记':'📍 开始标记'};
    wrap.onclick=e=>{if(!active||e.target!==img)return;let r=img.getBoundingClientRect();markerFormFull({new:true,x:(e.clientX-r.left)/r.width,y:(e.clientY-r.top)/r.height})};
    $('#replacePlanFull').onclick=async()=>{let k=await pickImage();if(k&&confirm('更换图纸但保留现有点位坐标？')){s.plan=k;save();floorPlanView()}};
    $('#addMarkerList').onclick=()=>markerFormFull({new:true,x:.5,y:.5});
  }
  function markerFormFull(m){form(m.new?'新增 Floor Plan 标记':'编辑 Floor Plan 标记',`<label>名称<input id="fmName" value="${esc(m.label||'')}" placeholder="例如：电视墙插座"></label><div class="two"><label>空间<select id="fmRoom">${roomOpts(m.room||'')}</select></label><label>类型<select id="fmType">${['插座','灯位','柜体','尺寸','水位','网络','智能家居','其他'].map(x=>`<option ${m.type===x?'selected':''}>${x}</option>`).join('')}</select></label></div><label>备注<textarea id="fmNote">${esc(m.note||'')}</textarea></label>${m.new?'':'<button class="danger" id="deleteMarkerFull" type="button">删除标记</button>'}`,()=>{let n=$('#fmName').value.trim();if(!n)return;let obj=m.new?{id:uid(),x:m.x,y:m.y}:m;Object.assign(obj,{label:n,room:$('#fmRoom').value,type:$('#fmType').value,note:$('#fmNote').value.trim()});if(m.new)s.markers.push(obj);save();floorPlanView()});if(!m.new)$('#deleteMarkerFull').onclick=()=>{s.markers=s.markers.filter(x=>x.id!==m.id);save();floorPlanView()}}

  // ---------- INSPIRATION ----------
  async function inspirationView(prefRoom=''){
    let cards='';for(const x of [...s.ideas].reverse()){let u=await anyMedia(x.imgKey||x.image||x.key);cards+=`<button class="mediaCard" data-idea-full="${x.id}">${u?`<img src="${u}">`:'<div class="mediaPh">🖼️</div>'}<span class="copy"><b>${esc(x.title||'参考灵感')}</b><small>${esc([x.room,x.note].filter(Boolean).join(' · '))}</small></span></button>`}
    modal('设计灵感库','参考图与“我要图里的什么”分开保存，不会混进 Shopping。',`<button class="primary" id="addIdeaFull">＋ 添加参考图</button><div class="mediaGrid" style="margin-top:12px">${cards||'<div class="empty" style="grid-column:1/-1">还没有灵感图</div>'}</div>`);$('#addIdeaFull').onclick=()=>inspirationForm(null,prefRoom);$$('[data-idea-full]').forEach(b=>b.onclick=()=>inspirationDetail(b.dataset.ideaFull))
  }
  function inspirationForm(x=null,prefRoom=''){let key=x?.imgKey||x?.image||'';form(x?'编辑灵感':'添加灵感',`<label>标题<input id="fiTitle" value="${esc(x?.title||'')}" placeholder="例如：主人房悬空电视柜"></label><label>空间<select id="fiRoom">${roomOpts(x?.room||prefRoom)}</select></label><button class="secondary" id="fiPick" type="button">${key?'✓ 已有图片':'📷 选择参考图'}</button><label>我要图里的什么？<textarea id="fiNote" placeholder="例如：只参考柜体比例和灯带，不照搬颜色">${esc(x?.note||'')}</textarea></label>${x?'<button class="danger" id="fiDelete" type="button">删除</button>':''}`,()=>{let title=$('#fiTitle').value.trim()||'参考灵感';let o=x||{id:uid()};Object.assign(o,{title,room:$('#fiRoom').value,note:$('#fiNote').value.trim(),imgKey:key});if(!x)s.ideas.push(o);save();inspirationView(o.room)});$('#fiPick').onclick=async()=>{let k=await pickImage();if(k){key=k;$('#fiPick').textContent='✓ 已选择图片'}};if(x)$('#fiDelete').onclick=()=>{s.ideas=s.ideas.filter(v=>v.id!==x.id);save();inspirationView()}}
  async function inspirationDetail(id){let x=s.ideas.find(v=>v.id===id);if(!x)return;let u=await anyMedia(x.imgKey||x.image);modal(x.title,x.room||'未指定空间',`${u?`<img src="${u}" style="width:100%;max-height:360px;object-fit:cover;border-radius:18px">`:''}<div class="card" style="margin-top:10px"><div class="kv"><span>我要参考</span><b>${esc(x.note||'未写')}</b></div></div><button class="secondary" id="editIdeaFull">编辑</button>`);$('#editIdeaFull').onclick=()=>inspirationForm(x)}

  // ---------- DEFECT EVIDENCE ----------
  function defectFormFull(d=null,prefRoom=''){let before=d?.before||'',after=d?.after||'';form(d?'编辑 Defect':'新增 Defect',`<label>问题<input id="fdTitle" value="${esc(d?.title||'')}"></label><div class="two"><label>空间<select id="fdRoom">${roomOpts(d?.room||prefRoom)}</select></label><label>状态<select id="fdStatus">${['待处理','处理中','待复验','已解决'].map(x=>`<option ${(d?.status||'待处理')===x?'selected':''}>${x}</option>`).join('')}</select></label></div><div class="two"><label>负责人<input id="fdOwner" value="${esc(d?.owner||'')}"></label><label>截止<input id="fdDue" type="date" value="${esc(d?.due||'')}"></label></div><div class="two"><button class="secondary" id="fdBefore" type="button">${before?'✓ Before 已有':'📷 Before'}</button><button class="secondary" id="fdAfter" type="button">${after?'✓ After 已有':'📷 After'}</button></div><label>说明<textarea id="fdNote">${esc(d?.note||'')}</textarea></label>${d?'<button class="danger" id="fdDelete" type="button">删除 Defect</button>':''}`,()=>{let title=$('#fdTitle').value.trim();if(!title)return;let o=d||{id:uid()};Object.assign(o,{title,room:$('#fdRoom').value,status:$('#fdStatus').value,owner:$('#fdOwner').value.trim(),due:$('#fdDue').value,note:$('#fdNote').value.trim(),before,after});if(!d)s.defects.unshift(o);save();tab='defect';fullSetControlActive('defect');render();renderControl()});$('#fdBefore').onclick=async()=>{let k=await pickImage();if(k){before=k;$('#fdBefore').textContent='✓ Before 已有'}};$('#fdAfter').onclick=async()=>{let k=await pickImage();if(k){after=k;$('#fdAfter').textContent='✓ After 已有'}};if(d)$('#fdDelete').onclick=()=>{if(confirm('删除这个 Defect？')){s.defects=s.defects.filter(x=>x.id!==d.id);save();$('#modal').classList.add('hide');render();renderControl()}}}
  async function defectDetailFull(id){let d=s.defects.find(x=>x.id===id);if(!d)return;let b=await anyMedia(d.before),a=await anyMedia(d.after);modal(d.title,d.room||'未指定空间',`<div class="defMedia">${b?`<img src="${b}">`:'<div class="placeholder">Before 待补</div>'}${a?`<img src="${a}">`:'<div class="placeholder">After 待补</div>'}</div><div class="card"><div class="kv"><span>状态</span><b>${esc(d.status||'待处理')}</b></div><div class="kv"><span>负责人</span><b>${esc(d.owner||'-')}</b></div><div class="kv"><span>说明</span><b>${esc(d.note||'-')}</b></div></div><button class="primary" id="fdResolve">${d.status==='已解决'?'恢复待复验':'标记已解决'}</button><button class="secondary" id="fdEdit">编辑 / 补照片</button>`);$('#fdResolve').onclick=()=>{d.status=d.status==='已解决'?'待复验':'已解决';save();render();defectDetailFull(id)};$('#fdEdit').onclick=()=>defectFormFull(d)}

  // ---------- CONTRACTORS ----------
  function contractorFormFull(c=null,prefRoom=''){let type=c?.type||c?.trade||'',contact=c?.contact||c?.phone||'',depositAmount=Number(c?.depositAmount||0);form(c?'编辑师傅 / 供应商':'新增师傅 / 供应商',`<label>姓名 / 公司<input id="fcName" value="${esc(c?.name||'')}"></label><div class="two"><label>类型 / 工种<input id="fcType" value="${esc(type)}" placeholder="电工 / 木工橱柜"></label><label>空间<select id="fcRoom">${roomOpts(c?.room||prefRoom)}</select></label></div><label>联系方式<input id="fcContact" value="${esc(contact)}"></label><div class="two"><label>报价 RM<input id="fcQuote" inputmode="decimal" value="${Number(c?.quote||0)||''}"></label><label>已付订金 RM<input id="fcDeposit" inputmode="decimal" value="${depositAmount||''}"></label></div><label>状态<select id="fcStatus">${['未开始','进行中','完成'].map(x=>`<option ${(c?.status||'未开始')===x?'selected':''}>${x}</option>`).join('')}</select></label><label>关键承诺 / 备注<textarea id="fcNote">${esc(c?.note||'')}</textarea></label>${c?'<button class="danger" id="fcDelete" type="button">删除师傅</button>':''}`,()=>{let n=$('#fcName').value.trim();if(!n)return;let o=c||{id:uid()};Object.assign(o,{name:n,type:$('#fcType').value.trim(),trade:$('#fcType').value.trim(),room:$('#fcRoom').value,contact:$('#fcContact').value.trim(),phone:$('#fcContact').value.trim(),quote:Number($('#fcQuote').value)||0,depositAmount:Number($('#fcDeposit').value)||0,status:$('#fcStatus').value,note:$('#fcNote').value.trim()});if(!c)s.contractors.push(o);if(o.quote&&!s.quotes.some(q=>q.contractorId===o.