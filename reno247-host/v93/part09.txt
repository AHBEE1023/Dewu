id)){s.quotes.push({id:uid(),contractorId:o.id,title:o.type||o.name,vendor:o.name,total:o.quote,room:o.room,status:'已选',compareGroup:o.type||'师傅报价'})}if(o.depositAmount&&!s.payments.some(p=>p.contractorId===o.id&&p.initial)){s.payments.push({id:uid(),contractorId:o.id,title:o.name+' Deposit',vendor:o.name,amount:o.depositAmount,date:day(),kind:'Deposit',initial:true})}save();tab='contractors';fullSetControlActive('contractors');render();renderControl()});if(c)$('#fcDelete').onclick=()=>{if(confirm('删除师傅资料？报价和付款不会自动删除。')){s.contractors=s.contractors.filter(x=>x.id!==c.id);save();$('#modal').classList.add('hide');render();renderControl()}}}
  function contractorDetailFull(id){let c=s.contractors.find(x=>x.id===id);if(!c)return;let qs=s.quotes.filter(q=>q.contractorId===id||q.vendor===c.name),ps=s.payments.filter(p=>p.contractorId===id||p.vendor===c.name),paid=ps.reduce((a,x)=>a+Number(x.amount||0),0),quoted=qs.filter(q=>q.status==='已选'||q.status==='已确认'||!q.status).reduce((a,x)=>a+Number(x.total||x.quote||0),0)||Number(c.quote||0);modal(c.name,c.type||c.trade||'师傅 / Supplier',`<div class="card"><div class="kv"><span>联系方式</span><b>${esc(c.contact||c.phone||'-')}</b></div><div class="kv"><span>报价 / 已选</span><b>${rm(quoted)}</b></div><div class="kv"><span>已付款</span><b>${rm(paid)}</b></div><div class="kv"><span>余额</span><b>${rm(Math.max(0,quoted-paid))}</b></div><div class="kv"><span>关键承诺</span><b>${esc(c.note||'-')}</b></div></div><div class="two"><button class="primary" id="fcAddQuote">＋报价</button><button class="secondary" id="fcAddPay">＋付款</button></div><button class="secondary" id="fcEdit">编辑资料</button>`);$('#fcAddQuote').onclick=()=>quoteFormFull(null,c);$('#fcAddPay').onclick=()=>paymentFormFull(null,c);$('#fcEdit').onclick=()=>contractorFormFull(c)}

  // ---------- QUOTES / PAYMENTS / EXPENSES ----------
  function quoteFormFull(q=null,c=null){form(q?'编辑报价':'新增报价',`<label>项目<input id="fqTitle" value="${esc(q?.title||c?.type||c?.trade||'')}" placeholder="例如：全屋橱柜"></label><div class="two"><label>供应商<input id="fqVendor" value="${esc(q?.vendor||c?.name||'')}"></label><label>空间<select id="fqRoom">${roomOpts(q?.room||c?.room||'')}</select></label></div><div class="two"><label>总价 RM<input id="fqTotal" inputmode="decimal" value="${Number(q?.total||q?.quote||0)||''}"></label><label>状态<select id="fqStatus">${['候选','已选','已确认','未选择'].map(x=>`<option ${(q?.status||'候选')===x?'selected':''}>${x}</option>`).join('')}</select></label></div><label>对比组<input id="fqGroup" value="${esc(q?.compareGroup||q?.group||c?.type||'')}" placeholder="例如：橱柜报价"></label><label>备注<textarea id="fqNote">${esc(q?.note||'')}</textarea></label>${q?'<button class="danger" id="fqDelete" type="button">删除报价</button>':''}`,()=>{let title=$('#fqTitle').value.trim();if(!title)return;let o=q||{id:uid()};Object.assign(o,{title,vendor:$('#fqVendor').value.trim(),room:$('#fqRoom').value,total:Number($('#fqTotal').value)||0,status:$('#fqStatus').value,compareGroup:$('#fqGroup').value.trim()||title,note:$('#fqNote').value.trim(),contractorId:q?.contractorId||c?.id||''});if(!q)s.quotes.unshift(o);if(o.status==='已选'||o.status==='已确认'){for(let z of s.quotes)if(z.id!==o.id&&z.compareGroup===o.compareGroup&&['已选','已确认'].includes(z.status))z.status='未选择'}save();tab='quotes';fullSetControlActive('quotes');render();renderControl()});if(q)$('#fqDelete').onclick=()=>{if(confirm('删除报价？')){s.quotes=s.quotes.filter(x=>x.id!==q.id);save();$('#modal').classList.add('hide');render();renderControl()}}}
  function quoteDetailFull(id){let q=s.quotes.find(x=>x.id===id);if(!q)return;let ps=s.payments.filter(x=>x.quoteId===id||(q.vendor&&x.vendor===q.vendor)),paid=ps.reduce((a,x)=>a+Number(x.amount||0),0);modal(q.title,q.vendor||'报价',`<div class="card"><div class="kv"><span>报价</span><b>${rm(q.total||q.quote)}</b></div><div class="kv"><span>状态</span><b>${esc(q.status||'候选')}</b></div><div class="kv"><span>对比组</span><b>${esc(q.compareGroup||'-')}</b></div><div class="kv"><span>已付</span><b>${rm(paid)}</b></div><div class="kv"><span>余额</span><b>${rm(Math.max(0,Number(q.total||q.quote||0)-paid))}</b></div></div><div class="two"><button class="primary" id="fqChoose">选择此报价</button><button class="secondary" id="fqPay">记录付款</button></div><button class="secondary" id="fqEdit">编辑报价</button>`);$('#fqChoose').onclick=()=>{q.status='已选';for(let z of s.quotes)if(z.id!==q.id&&z.compareGroup===q.compareGroup)z.status='未选择';save();render();quoteDetailFull(id)};$('#fqPay').onclick=()=>paymentFormFull(null,null,q);$('#fqEdit').onclick=()=>quoteFormFull(q)}
  function paymentFormFull(p=null,c=null,q=null){form(p?'编辑付款':'记录付款',`<label>项目<input id="fpTitle" value="${esc(p?.title||q?.title||c?.name||'')}" placeholder="例如：橱柜第二期"></label><div class="two"><label>供应商<input id="fpVendor" value="${esc(p?.vendor||q?.vendor||c?.name||'')}"></label><label>金额 RM<input id="fpAmount" inputmode="decimal" value="${Number(p?.amount||0)||''}"></label></div><div class="two"><label>日期<input id="fpDate" type="date" value="${esc(p?.date||day())}"></label><label>类型<select id="fpKind">${['Deposit','Progress Payment','Balance','全额','其他'].map(x=>`<option ${(p?.kind||'Progress Payment')===x?'selected':''}>${x}</option>`).join('')}</select></label></div><label>备注<textarea id="fpNote">${esc(p?.note||'')}</textarea></label>${p?'<button class="danger" id="fpDelete" type="button">删除付款</button>':''}`,()=>{let amount=Number($('#fpAmount').value)||0;if(!amount)return;let o=p||{id:uid()};Object.assign(o,{title:$('#fpTitle').value.trim()||'付款',vendor:$('#fpVendor').value.trim(),amount,date:$('#fpDate').value,kind:$('#fpKind').value,note:$('#fpNote').value.trim(),contractorId:p?.contractorId||c?.id||'',quoteId:p?.quoteId||q?.id||''});if(!p)s.payments.unshift(o);save();renderBudget();tab='payments';fullSetControlActive('payments');render();renderControl()});if(p)$('#fpDelete').onclick=()=>{if(confirm('删除付款记录？')){s.payments=s.payments.filter(x=>x.id!==p.id);save();$('#modal').classList.add('hide');render();renderControl()}}}
  function expenseFormFull(e=null,prefRoom=''){form(e?'编辑其他费用':'新增其他费用',`<label>项目<input id="feName" value="${esc(e?.name||'')}"></label><div class="two"><label>空间<select id="feRoom">${roomOpts(e?.room||prefRoom)}</select></label><label>金额 RM<input id="feAmount" inputmode="decimal" value="${Number(e?.amount||0)||''}"></label></div><label>日期<input id="feDate" type="date" value="${esc(e?.date||day())}"></label><label>类别<input id="feCat" value="${esc(e?.category||'')}"></label><label>备注<textarea id="feNote">${esc(e?.note||'')}</textarea></label>${e?'<button class="danger" id="feDelete" type="button">删除费用</button>':''}`,()=>{let n=$('#feName').value.trim(),a=Number($('#feAmount').value)||0;if(!n||!a)return;let o=e||{id:uid()};Object.assign(o,{name:n,room:$('#feRoom').value,amount:a,date:$('#feDate').value,category:$('#feCat').value.trim(),note:$('#feNote').value.trim()});if(!e)s.expenses.unshift(o);save();renderBudget();render()});if(e)$('#feDelete').onclick=()=>{if(confirm('删除费用？')){s.expenses=s.expenses.filter(x=>x.id!==e.id);save();$('#modal').classList.add('hide');render()}}}

  // ---------- DECISION / MATERIAL COMPARISON ----------
  function parseDecisionOptions(t){return String(t||'').split('\n').map(x=>x.trim()).filter(Boolean).map(line=>{let [name,price,note]=line.split('|').map(x=>x.trim());return{id:uid(),name,price:Number(price)||0,note:note||''}})}
  function decisionFormFull(d=null,prefRoom=''){let lines=(d?.options||[]).map(x=>[x.name,x.price||'',x.note||''].join(' | ')).join('\n');form(d?'编辑决定 / 材料选择':'新增决定 / 材料选择',`<label>事项<input id="fdecTitle" value="${esc(d?.title||'')}"></label><div class="two"><label>类型<select id="fdecKind"><option value="decision" ${(d?.kind||'decision')==='decision'?'selected':''}>一般决定</option><option value="material" ${d?.kind==='material'?'selected':''}>材料 / 方案对比</option></select></label><label>空间<select id="fdecRoom">${roomOpts(d?.room||prefRoom)}</select></label></div><div class="two"><label>优先级<select id="fdecPri">${['高','普通','低'].map(x=>`<option ${(d?.priority||'普通')===x?'selected':''}>${x}</option>`).join('')}</select></label><label>最迟确认<input id="fdecDue" type="date" value="${esc(d?.due||'')}"></label></div><label>会阻塞什么<input id="fdecBlock" value="${esc(d?.block||'')}"></label><label>选项（每行：名称 | 价格 | 备注）<textarea id="fdecOptions" placeholder="方案A | 1200 | Quartz\n方案B | 900 | Sintered stone">${esc(lines)}</textarea></label><label>备注<textarea id="fdecNote">${esc(d?.note||'')}</textarea></label>`,()=>{let title=$('#fdecTitle').value.trim();if(!title)return;let o=d||{id:uid(),status:'待确认'};Object.assign(o,{title,kind:$('#fdecKind').value,room:$('#fdecRoom').value,priority:$('#fdecPri').value,due:$('#fdecDue').value,block:$('#fdecBlock').value.trim(),note:$('#fdecNote').value.trim(),options:parseDecisionOptions($('#fdecOptions').value)});if(!d)s.decisions.unshift(o);save();tab=o.kind==='material'?'materials':'decision';fullSetControlActive(tab);render();renderControl()})}
  function decisionDetailFull(id){let d=s.decisions.find(x=>x.id===id);if(!d)return;let opts=(d.options||[]).map(x=>`<div class="optionCard ${d.chosenOption===x.id?'chosen':''}"><div class="topline"><b>${esc(x.name)}</b><b>${x.price?rm(x.price):''}</b></div><small>${esc(x.note||'')}</small><button class="linkbtn" data-choose-opt="${x.id}" style="margin-top:8px">${d.chosenOption===x.id?'✓ 已选':'选择这个'}</button></div>`).join('');modal(d.title,[d.room,d.due?('截止 '+d.due):'',d.block?('阻塞 '+d.block):''].filter(Boolean).join(' · '),`${opts||'<div class="empty">没有方案选项，可编辑后加入材料/方案比较。</div>'}<div class="card"><div class="kv"><span>状态</span><b>${esc(d.status||'待确认')}</b></div><div class="kv"><span>备注</span><b>${esc(d.note||'-')}</b></div></div><button class="secondary" id="fdecEdit">编辑</button>`);$$('[data-choose-opt]').forEach(b=>b.onclick=()=>{d.chosenOption=b.dataset.chooseOpt;d.status='已确认';save();render();decisionDetailFull(id)});$('#fdecEdit').onclick=()=>decisionFormFull(d)}

  // ---------- SITE LOG / ROOM PHOTOS ----------
  function siteLogFormFull(l=null,prefRoom=''){let photos=[...(l?.photos||[])];form(l?'编辑施工日志':'施工日志',`<div class="two"><label>日期<input id="flDate" type="date" value="${esc(l?.date||day())}"></label><label>空间<select id="flRoom">${roomOpts(l?.room||prefRoom)}</select></label></div><label>今天做了什么<textarea id="flWork">${esc(l?.work||'')}</textarea></label><label>问题 / 下一步<textarea id="flNote">${esc(l?.note||'')}</textarea></label><button class="secondary" id="flPhoto" type="button">📷 添加现场照片（${photos.length}）</button>`,()=>{let o=l||{id:uid()};Object.assign(o,{date:$('#flDate').value,room:$('#flRoom').value,work:$('#flWork').value.trim(),note:$('#flNote').value.trim(),photos});if(!l)s.siteLogs.unshift(o);save();tab='log';fullSetControlActive('log');render();renderControl()});$('#flPhoto').onclick=async()=>{let k=await pickImage();if(k){photos.push(k);$('#flPhoto').textContent='📷 添加现场照片（'+photos.length+'）'}}}
  async function siteLogDetailFull(id){let l=s.siteLogs.find(x=>x.id===id);if(!l)return;let media='';for(const k of l.photos||[]){let u=await anyMedia(k);if(u)media+=`<img src="${u}" style="width:112px;height:90px;border-radius:14px;object-fit:cover">`}modal(l.date,l.room||'现场日志',`${media?`<div class="galleryStrip">${media}</div>`:''}<div class="card" style="margin-top:10px"><div class="kv"><span>施工内容</span><b>${esc(l.work||'-')}</b></div><div class="kv"><span>问题 / 下一步</span><b>${esc(l.note||'-')}</b></div></div><button class="secondary" id="flEdit">编辑日志</button>`);$('#flEdit').onclick=()=>siteLogFormFull(l)}
  function roomPhotoForm(prefRoom='',x=null){let key=x?.imgKey||'';form(x?'编辑现场照片':'上传现场照片',`<label>空间<select id="frpRoom">${roomOpts(x?.room||prefRoom)}</select></label><label>日期<input id="frpDate" type="date" value="${esc(x?.date||day())}"></label><button class="secondary" id="frpPick" type="button">${key?'✓ 已有照片':'📷 选择照片'}</button><label>说明<textarea id="frpNote">${esc(x?.note||'')}</textarea></label>`,()=>{if(!key)return alert('请先选择照片');let o=x||{id:uid()