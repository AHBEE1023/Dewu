)};Object.assign(o,{room:$('#frpRoom').value,date:$('#frpDate').value,note:$('#frpNote').value.trim(),imgKey:key});if(!x)s.roomPhotos.unshift(o);save();if(o.room)roomHubFull(o.room,'site');else{tab='photos';fullSetControlActive('photos');renderControl()}});$('#frpPick').onclick=async()=>{let k=await pickImage();if(k){key=k;$('#frpPick').textContent='✓ 已选择照片'}}}
  async function roomPhotoDetailFull(id){let x=s.roomPhotos.find(v=>v.id===id);if(!x)return;let u=await anyMedia(x.imgKey);modal(x.room||'现场照片',x.date||'',`${u?`<img src="${u}" style="width:100%;max-height:420px;object-fit:cover;border-radius:18px">`:''}<p class="moduleHint">${esc(x.note||'')}</p><button class="secondary" id="frpEdit">编辑</button><button class="danger" id="frpDelete">删除</button>`);$('#frpEdit').onclick=()=>roomPhotoForm(x.room,x);$('#frpDelete').onclick=()=>{s.roomPhotos=s.roomPhotos.filter(v=>v.id!==id);save();$('#modal').classList.add('hide');render()}}

  // ---------- INSPECTION / VARIATION / HANDOVER ----------
  function inspectionDetailFull(id){let x=s.inspections.find(v=>v.id===id);if(!x)return;modal(x.title,x.room||'阶段验收',`<div class="card"><div class="kv"><span>状态</span><b>${esc(x.status||'待检查')}</b></div><div class="kv"><span>计划日期</span><b>${esc(x.due||'-')}</b></div><div class="kv"><span>验收说明</span><b>${esc(x.note||'-')}</b></div></div><div class="fullActions"><button id="finPass">✓ 通过</button><button id="finFail">✕ 不通过 → Defect</button></div>`);$('#finPass').onclick=()=>{x.status='通过';save();render();inspectionDetailFull(id)};$('#finFail').onclick=()=>{x.status='不通过';let title='验收不通过：'+x.title;if(!s.defects.some(d=>d.sourceInspectionId===x.id))s.defects.unshift({id:uid(),sourceInspectionId:x.id,title,room:x.room||'',owner:'',status:'待处理',note:x.note||'由验收自动生成',due:''});save();render();tab='defect';$('#modal').classList.add('hide');fullSetControlActive('defect');go('control');renderControl()}}
  function variationDetailFull(id){let x=s.variations.find(v=>v.id===id);if(!x)return;modal(x.title,x.room||'工程变更',`<div class="card"><div class="kv"><span>金额影响</span><b>${rm(x.amount)}</b></div><div class="kv"><span>工期影响</span><b>${Number(x.days||0)} 天</b></div><div class="kv"><span>状态</span><b>${esc(x.status||'待批准')}</b></div><div class="kv"><span>说明</span><b>${esc(x.note||'-')}</b></div></div><button class="primary" id="fvApprove">${x.status==='已批准'?'撤回批准':'批准变更'}</button>`);$('#fvApprove').onclick=()=>{x.status=x.status==='已批准'?'待批准':'已批准';save();render();variationDetailFull(id)}}
  function handoverDetailFull(id){let x=s.handover.find(v=>v.id===id);if(!x)return;modal(x.title,x.category||'交付 / Warranty',`<div class="card"><div class="kv"><span>供应商</span><b>${esc(x.vendor||'-')}</b></div><div class="kv"><span>交付日期</span><b>${esc(x.due||'-')}</b></div><div class="kv"><span>Warranty 到期</span><b>${esc(x.expiry||'-')}</b></div><div class="kv"><span>状态</span><b>${esc(x.status||'待收齐')}</b></div><div class="kv"><span>备注</span><b>${esc(x.note||'-')}</b></div></div><button class="primary" id="fhDone">${['已收齐','完成'].includes(x.status)?'恢复待收齐':'标记已收齐'}</button>`);$('#fhDone').onclick=()=>{x.status=['已收齐','完成'].includes(x.status)?'待收齐':'已收齐';save();render();handoverDetailFull(id)}}

  // ---------- SCHEDULE / REMINDERS / TIMELINE ----------
  function scheduleFormFull(x=null,prefRoom=''){form(x?'编辑施工日程':'新增施工日程',`<label>事项<input id="fsTitle" value="${esc(x?.title||'')}"></label><div class="two"><label>空间<select id="fsRoom">${roomOpts(x?.room||prefRoom)}</select></label><label>师傅 / 公司<input id="fsVendor" value="${esc(x?.vendor||'')}"></label></div><div class="two"><label>开始<input id="fsStart" type="date" value="${esc(x?.start||'')}"></label><label>结束<input id="fsEnd" type="date" value="${esc(x?.end||'')}"></label></div><label>状态<select id="fsStatus">${['计划','已确认','施工中','完成'].map(v=>`<option ${(x?.status||'计划')===v?'selected':''}>${v}</option>`).join('')}</select></label><label>备注<textarea id="fsNote">${esc(x?.note||'')}</textarea></label>`,()=>{let title=$('#fsTitle').value.trim();if(!title)return;let o=x||{id:uid()};Object.assign(o,{title,room:$('#fsRoom').value,vendor:$('#fsVendor').value.trim(),start:$('#fsStart').value,end:$('#fsEnd').value,status:$('#fsStatus').value,note:$('#fsNote').value.trim()});if(!x)s.schedule.push(o);save();tab='schedule';fullSetControlActive('schedule');render();renderControl()})}
  function reminderFormFull(x=null){form(x?'编辑付款提醒':'新增付款提醒',`<label>事项<input id="fprTitle" value="${esc(x?.title||'')}"></label><div class="two"><label>金额 RM<input id="fprAmount" inputmode="decimal" value="${Number(x?.amount||0)||''}"></label><label>到期<input id="fprDue" type="date" value="${esc(x?.due||'')}"></label></div><label>供应商<input id="fprVendor" value="${esc(x?.vendor||'')}"></label><label>备注<textarea id="fprNote">${esc(x?.note||'')}</textarea></label>`,()=>{let title=$('#fprTitle').value.trim();if(!title)return;let o=x||{id:uid(),status:'待付款'};Object.assign(o,{title,amount:Number($('#fprAmount').value)||0,due:$('#fprDue').value,vendor:$('#fprVendor').value.trim(),note:$('#fprNote').value.trim()});if(!x)s.paymentReminders.push(o);save();tab='reminder';fullSetControlActive('reminder');render();renderControl()})}
  function timelineItems(){let a=[];for(const x of s.schedule)a.push({date:x.start||x.end||'',title:x.title,sub:[x.room,x.vendor,x.status].filter(Boolean).join(' · '),ico:'📅'});for(const x of s.tasks.filter(x=>x.due))a.push({date:x.due,title:x.name,sub:[x.room,x.done?'已完成':'任务截止'].filter(Boolean).join(' · '),ico:'✅'});for(const x of s.inspections.filter(x=>x.due))a.push({date:x.due,title:x.title,sub:[x.room,'验收'].filter(Boolean).join(' · '),ico:'✓'});for(const x of s.paymentReminders.filter(x=>x.due))a.push({date:x.due,title:x.title,sub:rm(x.amount)+' · '+(x.status||'待付款'),ico:'💳'});for(const x of s.shopping.filter(x=>x.expected))a.push({date:x.expected,title:x.item,sub:'采购 / 到货 · '+x.status,ico:'🛒'});return a.filter(x=>x.date).sort((a,b)=>a.date.localeCompare(b.date))}

  // ---------- BUDGET FULL ----------
  renderBudget=function(){
    let t=totals();$('#remain').textContent=rm(s.budget-t.final);$('#bbudget').textContent=rm(s.budget);$('#bcommit').textContent=rm(t.commit);$('#bpaid').textContent=rm(t.actual);$('#bfinal').textContent=rm(t.final);$('#moneyNote').textContent=`候选商品与候选报价不进入承诺；已选报价、已选商品、批准变更与实际支出才影响预计结算。`;
    let buckets=[...(s.budgetBuckets||[])].sort((a,b)=>(Number(b.spent||0)/Math.max(1,Number(b.budget||0)))-(Number(a.spent||0)/Math.max(1,Number(a.budget||0))));let bb=$('#budgetBuckets');if(bb)bb.innerHTML=buckets.map(x=>{let p=x.budget?Math.round(Number(x.spent||0)/Number(x.budget||0)*100):0;return`<div class="bucket"><small>${esc(x.name)}</small><b>${rm(x.spent||0)} / ${rm(x.budget||0)}</b><strong class="${p>=80?'warn':''}">${p}%</strong><div class="bucketUsage"><i style="width:${Math.min(100,p)}%"></i></div><em>${esc(x.note||'')}</em></div>`}).join('');
    const cash=s.paymentReminders.filter(x=>x.status!=='已付款'&&x.due&&dateDelta(x.due)>=0&&dateDelta(x.due)<=30).reduce((a,x)=>a+Number(x.amount||0),0)+s.shopping.filter(x=>x.expected&&dateDelta(x.expected)>=0&&dateDelta(x.expected)<=30&&['已选','等优惠'].includes(x.status)).reduce((a,x)=>a+landed(x),0);
    let body='';if(btab==='quotes'){let groups={};s.quotes.forEach(q=>(groups[q.compareGroup||q.title||'其他']??=[]).push(q));body=`<button class="controlAdd" id="fbAddQuote">＋ 新增 / 候选报价</button><div class="restoreNotice">报价比较功能保留：同一“对比组”可以放多家报价，只有已选/已确认才进入预计承诺。</div>${Object.entries(groups).map(([g,arr])=>`<div class="shopGroupTitle"><b>${esc(g)}</b><small>${arr.length} 份</small></div><div class="quoteCompare">${arr.map(q=>`<button class="quoteRow ${['已选','已确认'].includes(q.status)?'selected':''}" data-fquote="${q.id}"><div class="topline"><b>${esc(q.vendor||q.title)}</b><b>${rm(q.total||q.quote)}</b></div><small>${esc([q.room,q.status,q.note].filter(Boolean).join(' · '))}</small></button>`).join('')}</div>`).join('')||empty()}`}
    if(btab==='payments')body=`<button class="controlAdd" id="fbAddPayment">＋ 记录付款</button><div class="restoreNotice">未来 30 天已知现金流约 ${rm(cash)}（付款提醒 + 已选待买商品）。</div>${s.payments.map(p=>`<button class="row" data-fpay="${p.id}"><span>💳</span><span class="main"><b>${esc(p.title||p.vendor||p.kind||'付款')}</b><small>${esc([p.vendor,p.kind,p.date,p.note].filter(Boolean).join(' · '))}</small></span><span class="amount">${rm(p.amount)}</span></button>`).join('')||empty()}`;
    if(btab==='expenses')body=`<button class="controlAdd" id="fbAddExpense">＋ 新增其他费用</button>${s.expenses.map(e=>`<button class="row" data-fexpense="${e.id}"><span>💰</span><span class="main"><b>${esc(e.name)}</b><small>${esc([e.category,e.room,e.date,e.note].filter(Boolean).join(' · '))}</small></span><span class="amount">${rm(e.amount)}</span></button>`).join('')||empty()}`;
    $('#budgetList').innerHTML=body;if($('#fbAddQuote'))$('#fbAddQuote').onclick=()=>quoteFormFull();if($('#fbAddPayment'))$('#fbAddPayment').onclick=()=>paymentFormFull();if($('#fbAddExpense'))$('#fbAddExpense').onclick=()=>expenseFormFull();$$('[data-fquote]').forEach(b=>b.onclick=()=>quoteDetailFull(b.dataset.fquote));$$('[data-fpay]').forEach(b=>{let x=s.payments.find(v=>v.id===b.dataset.fpay);b.onclick=()=>paymentFormFull(x)});$$('[data-fexpense]').forEach(b=>{let x=s.expenses.find(v=>v.id===b.dataset.fexpense);b.onclick=()=>expenseFormFull(x)})
  };

  // ---------- ROOM DIGITAL TWIN FULL ----------
  async function roomHubFull(r,rtab='overview'){
    let d=s.roomData[r]||{},tasks=s.tasks.filter(x=>x.room===r),decs=s.decisions.filter(x=>x.room===r),defs=s.defects.filter(x=>x.room===r),shops=s.shopping.filter(x=>x.room===r),logs=s.siteLogs.filter(x=>x.room===r),ins=s.inspections.filter(x=>x.room===r),socks=s.sockets.filter(x=>x.room===r),ideas=s.ideas.filter(x=>x.room===r),photos=s.roomPhotos.filter(x=>x.room===r),cons=s.contractors.filter(x=>x.room===r),quotes=s.quotes.filter(x=>x.room===r),expenses=s.expenses.filter(x=>x.room===r),markers=s.markers.filter(x=>x.room===r);let body='';
    if(rtab==='overview'){body=`<div class="roomMetrics"><div><small>房间进度</small><b>${d.progress||0}%</b></div><div><small>待办</small><b>${tasks.filter(x=>!x.done).length}</b></div><div><small>待确认</small><b>${decs.filter(x=>x.status!=='已确认').length}</b></div><div><small>Defect</small><b>${defs.filter(x=>x.status!=='已解决').length}</b></div></div><div class="card"><div class="kv"><span>尺寸 / 重点</span><b>${esc(d.dimensions||'未记录')}</b></div><div class="kv"><span>设计方向</span><b>${esc(d.design||'未记录')}</b></div><div class="kv"><span>现场备注</span><b>${esc(d.note||'未记录')}</b></div></div><div class="roomHubActions"><button id="rfEdit">编辑资料</button><button id="rfTask">＋任务</button><button id="rfShop">＋商品</button></div>`}
    if(rtab==='design'){let ideaCards='';for(const x of ideas){let u=await anyMedia(x.imgKey||x.image);ideaCards+=`<button class="mediaCard" data-ridea="${x.id}">${u?`<img src="${u}">`:'<div class="mediaPh">🖼️</div>'}<span class="copy"><b>${esc(x.title||'灵感')}</b><small>${esc(x.note||'')}</small></span></button>`}body=`<div class="head"><h3>设计 / 灵感</h3><button id="rfIdea">＋参考图</button></div><div class="mediaGrid">${ideaCards||'<div class="empty" style="grid-column:1/-1">这个房间还没有灵感图</div>'}</div><section class="section"><div class="head"><h3>Floor Plan 点位</h3><button id="rfPlan">打开图纸</button></div>${markers.map(x=>`<div class="row"><span>📍</span><span class="main"><b>${esc(x.label)}</b><small>${esc([x.type,x.note].filter(Boolean).join(' · '))}</small></span></div>`).join('')||empty('暂无点位')}</section>`}
    if(rtab==='finance'){let qsum=quotes.filter(q=>['已选','已确认'].includes(q.status)).reduce((a,x)=>a+Number(x.total||0),0),esum=expenses.reduce((a,x)=>a+Number(x.amount||0),0),paid=s.payments.filter(p=>quotes.some(q=>q.id===p.quoteId)||(p.vendor&&cons.some(c=>c.name===p.vendor))).reduce((a,x)=>a+Number(x.amount||0),0);body=`<div class="roomFinance"><div><small>已选报价</small><b>${rm(qsum)}</b></div><div><small>其他费用</small><b>${rm(esum)}</b></div><div><small>相关付款</small><b>${rm(paid)}</