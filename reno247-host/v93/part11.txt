b></div><div><small>师傅</small><b>${cons.length} 位</b></div></div><div class="fullActions"><button id="rfCon">＋师傅</button><button id="rfQuote">＋报价</button><button id="rfExpense">＋费用</button><button id="rfPay">＋付款</button></div>${cons.map(c=>`<button class="row" data-rcon="${c.id}"><span>👷</span><span class="main"><b>${esc(c.name)}</b><small>${esc([c.type||c.trade,c.contact||c.phone].filter(Boolean).join(' · '))}</small></span></button>`).join('')}${quotes.map(q=>`<button class="row" data-rquote="${q.id}"><span>🧾</span><span class="main"><b>${esc(q.vendor||q.title)}</b><small>${esc(q.status||'候选')}</small></span><span class="amount">${rm(q.total)}</span></button>`).join('')}`}
    if(rtab==='actions'){body=`<div class="head"><h3>决定、任务与 Defect</h3><button id="rfDecision">＋待确认</button></div>${[...decs.map(x=>['◇',x.title,x.due?('截止 '+x.due):(x.note||''),x.status||'待确认']),...tasks.map(x=>['✅',x.name,x.due?('截止 '+x.due):(x.note||''),x.done?'已完成':'待处理']),...defs.map(x=>['⚠️',x.title,x.owner||x.note||'',x.status||'待处理'])].map(x=>`<div class="row"><span>${x[0]}</span><span class="main"><b>${esc(x[1])}</b><small>${esc(x[2])}</small></span>${pill(x[3])}</div>`).join('')||empty()}`}
    if(rtab==='shopping'){body=`<div class="head"><h3>这个房间的选购</h3><button id="rfShop2">＋收藏</button></div><div id="rfShopList">加载中…</div>`}
    if(rtab==='electrical'){body=`<div class="head"><h3>电位 / 网络点</h3><button id="rfPlan2">Floor Plan</button></div>${socks.map(x=>`<div class="row"><span>🔌</span><span class="main"><b>${esc(x.title)}</b><small>${esc([x.position,x.qty,x.type,x.note].filter(Boolean).join(' · '))}</small></span>${pill(x.status||'待确认')}</div>`).join('')||empty()}`}
    if(rtab==='site'){let ph='';for(const x of photos.slice(0,10)){let u=await anyMedia(x.imgKey);ph+=`<button data-rphoto="${x.id}">${u?`<img src="${u}">`:'<div class="ph">📷</div>'}</button>`}body=`<div class="head"><h3>现场照片</h3><button id="rfPhoto">＋照片</button></div><div class="galleryStrip">${ph||'<div class="empty" style="min-width:100%">还没有现场照片</div>'}</div><section class="section"><div class="head"><h3>施工日志</h3><button id="rfLog">＋日志</button></div>${logs.map(x=>`<button class="row" data-rlog="${x.id}"><span>📝</span><span class="main"><b>${esc(x.date||'施工日志')}</b><small>${esc(x.work||x.note||'')}</small></span><span class="photoCount">${(x.photos||[]).length}</span></button>`).join('')||empty()}</section>`}
    if(rtab==='inspect'){body=`<div class="head"><h3>验收 & Defect</h3><button id="rfInspect">＋验收</button></div>${ins.map(x=>`<button class="row" data-rinspect="${x.id}"><span>✓</span><span class="main"><b>${esc(x.title)}</b><small>${esc(x.note||'')}</small></span>${pill(x.status||'待检查')}</button>`).join('')||empty('还没有验收记录')}<section class="section"><h3>Defect</h3>${defs.map(x=>`<button class="row" data-rdef="${x.id}"><span>⚠️</span><span class="main"><b>${esc(x.title)}</b><small>${esc(x.note||'')}</small></span>${pill(x.status||'待处理')}</button>`).join('')||empty()}</section>`}
    modal(r,'Room Digital Twin · 功能完整保留，资料按房间自动归档',`<div class="roomHubTop"><div class="roomHubIcon">${EM[r]||'🏠'}</div><div><b>${esc(r)}</b><small>${d.progress||0}% · ${tasks.filter(x=>!x.done).length} 待办 · ${shops.length} 选购 · ${socks.length} 电位 · ${photos.length} 照片 · ${defs.filter(x=>x.status!=='已解决').length} Defect</small><div class="bar"><i style="width:${d.progress||0}%"></i></div></div></div><div class="tabs">${[['overview','总览'],['design','设计'],['finance','费用'],['actions','事项'],['shopping','选购'],['electrical','电位'],['site','现场'],['inspect','验收']].map(x=>`<button class="${rtab===x[0]?'on':''}" data-rfull="${x[0]}">${x[1]}</button>`).join('')}</div>${body}`);
    $$('[data-rfull]').forEach(b=>b.onclick=()=>roomHubFull(r,b.dataset.rfull));if($('#rfEdit'))$('#rfEdit').onclick=()=>roomEdit(r);if($('#rfTask'))$('#rfTask').onclick=()=>add('task',r);if($('#rfShop'))$('#rfShop').onclick=()=>productForm(null,r);if($('#rfIdea'))$('#rfIdea').onclick=()=>inspirationForm(null,r);if($('#rfPlan'))$('#rfPlan').onclick=floorPlanView;if($('#rfCon'))$('#rfCon').onclick=()=>contractorFormFull(null,r);if($('#rfQuote'))$('#rfQuote').onclick=()=>quoteFormFull(null,{room:r});if($('#rfExpense'))$('#rfExpense').onclick=()=>expenseFormFull(null,r);if($('#rfPay'))$('#rfPay').onclick=()=>paymentFormFull();if($('#rfDecision'))$('#rfDecision').onclick=()=>decisionFormFull(null,r);if($('#rfShop2'))$('#rfShop2').onclick=()=>productForm(null,r);if($('#rfPlan2'))$('#rfPlan2').onclick=floorPlanView;if($('#rfPhoto'))$('#rfPhoto').onclick=()=>roomPhotoForm(r);if($('#rfLog'))$('#rfLog').onclick=()=>siteLogFormFull(null,r);if($('#rfInspect'))$('#rfInspect').onclick=()=>add('inspect',r);if(rtab==='shopping')productRows(shops).then(h=>{let e=$('#rfShopList');if(e){e.innerHTML=h||empty();$$('[data-prod]').forEach(b=>b.onclick=()=>productDetail(b.dataset.prod))}});$$('[data-rcon]').forEach(b=>b.onclick=()=>contractorDetailFull(b.dataset.rcon));$$('[data-rquote]').forEach(b=>b.onclick=()=>quoteDetailFull(b.dataset.rquote));$$('[data-rphoto]').forEach(b=>b.onclick=()=>roomPhotoDetailFull(b.dataset.rphoto));$$('[data-rlog]').forEach(b=>b.onclick=()=>siteLogDetailFull(b.dataset.rlog));$$('[data-rinspect]').forEach(b=>b.onclick=()=>inspectionDetailFull(b.dataset.rinspect));$$('[data-rdef]').forEach(b=>b.onclick=()=>defectDetailFull(b.dataset.rdef));$$('[data-ridea]').forEach(b=>b.onclick=()=>inspirationDetail(b.dataset.ridea))
  }
  roomHub=roomHubFull;

  // ---------- CONTROL CENTER FULL ----------
  function fullTabs(){const tabs=$('#ctabs');if(!tabs)return;const list=[['overview','总览'],['task','任务'],['decision','待确认'],['materials','材料'],['defect','缺陷'],['inspect','验收'],['variation','变更'],['log','日志'],['photos','照片'],['shopping','选购'],['inspiration','灵感'],['plan','图纸'],['contractors','师傅'],['quotes','报价'],['payments','付款'],['schedule','日程'],['timeline','时间线'],['reminder','提醒'],['sockets','电位'],['handover','交付']];tabs.innerHTML=list.map(([id,label])=>`<button data-tab="${id}">${label}</button>`).join('');$$('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;fullSetControlActive(tab);renderControl()});if(!list.some(x=>x[0]===tab))tab='overview';fullSetControlActive(tab)}
  const control922=renderControl;
  renderControl=function(){updateControlHero();
    if(tab==='overview'){const data=[['task','✅','任务',fullOpenCount('task')],['decision','◇','待确认',fullOpenCount('decision')],['defect','⚠️','Defect',fullOpenCount('defect')],['inspect','✓','验收',fullOpenCount('inspect')],['shopping','🛒','选购',s.shopping.length],['inspiration','🖼️','灵感',s.ideas.length],['plan','📐','Floor Plan',s.markers.length],['contractors','👷','师傅',s.contractors.length],['quotes','🧾','报价',s.quotes.length],['payments','💳','付款',s.payments.length],['schedule','📅','施工日程',s.schedule.length],['photos','📷','现场照片',s.roomPhotos.length],['sockets','🔌','电位',s.sockets.length],['handover','🔑','交付/Warranty',s.handover.length]];$('#controlKicker').textContent='完整功能 × 真实资料';$('#controlTitle').textContent='控制中心';$('#controlDesc').textContent='不删功能，只把真实内容分流到正确模块。';$('#controlList').innerHTML=`<div class="restoreNotice">V9.3 已恢复 Floor Plan、灵感、现场照片、Defect Before/After、师傅详情、报价比较、付款、施工日程、提醒与时间线。</div><div class="controlGrid">${data.map(x=>`<button class="controlTile ${(['decision','defect','inspect'].includes(x[0])&&x[3])?'attn':''}" data-fulljump="${x[0]}"><span class="ico">${x[1]}</span><b>${x[3]}</b><span>${x[2]}</span><small>打开完整模块</small></button>`).join('')}</div>`;$$('[data-fulljump]').forEach(b=>b.onclick=()=>{tab=b.dataset.fulljump;fullSetControlActive(tab);renderControl()});return}
    if(tab==='decision'||tab==='materials'){let arr=s.decisions.filter(x=>tab==='materials'?(x.kind==='material'||(x.options||[]).length):x.kind!=='material');$('#controlKicker').textContent=tab==='materials'?'Selections × Compare':'Decision Inbox';$('#controlTitle').textContent=tab==='materials'?'材料 / 方案对比':'待确认事项';$('#controlDesc').textContent='保留截止日、阻塞关系与选择理由。';$('#controlList').innerHTML=`<button class="controlAdd" id="fdecAdd">＋ 新增${tab==='materials'?'材料 / 方案':'待确认'}</button>${arr.map(x=>`<button class="row" data-fdecision="${x.id}"><span>${tab==='materials'?'▧':'◇'}</span><span class="main"><b>${esc(x.title)}</b><small>${esc([x.room,x.due?('截止 '+x.due):'',x.block?('阻塞 '+x.block):'',x.chosenOption?'已选方案':''].filter(Boolean).join(' · '))}</small></span>${pill(x.status||'待确认')}</button>`).join('')||empty()}`;$('#fdecAdd').onclick=()=>decisionFormFull(tab==='materials'?{kind:'material',options:[]}:null);$$('[data-fdecision]').forEach(b=>b.onclick=()=>decisionDetailFull(b.dataset.fdecision));return}
    if(tab==='defect'){let arr=[...s.defects].sort((a,b)=>(a.status==='已解决')-(b.status==='已解决'));$('#controlKicker').textContent='Evidence × Before/After';$('#controlTitle').textContent='Defect';$('#controlDesc').textContent='复验只是状态，照片证据也要保留。';$('#controlList').innerHTML=`<button class="controlAdd" id="fdAdd">＋ 新增 Defect</button>${arr.map(x=>`<button class="row" data-fdefect="${x.id}"><span>⚠️</span><span class="main"><b>${esc(x.title)}</b><small>${esc([x.room,x.owner,x.note].filter(Boolean).join(' · '))}</small></span>${pill(x.status||'待处理')}</button>`).join('')||empty()}`;$('#fdAdd').onclick=()=>defectFormFull();$$('[data-fdefect]').forEach(b=>b.onclick=()=>defectDetailFull(b.dataset.fdefect));return}
    if(tab==='inspect'){let arr=[...s.inspections];$('#controlKicker').textContent='Stage Gate × Evidence';$('#controlTitle').textContent='验收';$('#controlDesc').textContent='不通过可直接生成 Defect。';$('#controlList').innerHTML=`<button class="controlAdd" id="finAdd">＋ 新增验收</button>${arr.map(x=>`<button class="row" data-fin="${x.id}"><span>✓</span><span class="main"><b>${esc(x.title)}</b><small>${esc([x.room,x.due,x.note].filter(Boolean).join(' · '))}</small></span>${pill(x.status||'待检查')}</button>`).join('')||empty()}`;$('#finAdd').onclick=()=>add('inspect');$$('[data-fin]').forEach(b=>b.onclick=()=>inspectionDetailFull(b.dataset.fin));return}
    if(tab==='variation'){let arr=s.variations;$('#controlKicker').textContent='Variation Order';$('#controlTitle').textContent='工程变更';$('#controlDesc').textContent='只有批准后的变更才影响预算预测。';$('#controlList').innerHTML=`<button class="controlAdd" id="fvAdd">＋ 新增工程变更</button>${arr.map(x=>`<button class="row" data-fvariation="${x.id}"><span>±</span><span class="main"><b>${esc(x.title)}</b><small>${esc([x.room,x.note,Number(x.days||0)?('工期 '+x.days+'天'):''].filter(Boolean).join(' · '))}</small></span><span class="amount">${rm(x.amount)}</span>${pill(x.status||'待批准')}</button>`).join('')||empty()}`;$('#fvAdd').onclick=()=>add('variation');$$('[data-fvariation]').forEach(b=>b.onclick=()=>variationDetailFull(b.dataset.fvariation));return}
    if(tab==='log'){let arr=s.siteLogs;$('#controlKicker').textContent='Daily Log × Photos';$('#controlTitle').textContent='施工日志';$('#controlDesc').textContent='每天做了什么、问题和照片都保留。';$('#controlList').innerHTML=`<button class="controlAdd" id="flAdd">＋ 新增施工日志</button>${arr.map(x=>`<button class="row" data-flog="${x.id}"><span>📝</span><span class="main"><b>${esc(x.date||'施工日志')} ${x.room?('· '+esc(x.room)):''}</b><small>${esc(x.work||x.note||'')}</small></span><span class="photoCount">${(x.photos||[]).length}</span></button>`).join('')||empty()}`;$('#flAdd').onclick=()=>siteLogFormFull();$$('[data-flog]').forEach(b=>b.onclick=()=>siteLogDetailFull(b.dataset.flog));return}
    if(tab==='photos'){(async()=>{let rows='';for(const x of s.roomPhotos){let u=await anyMedia(x.imgKey);rows+=`<button class="mediaCard" data-fphoto="${x.id}">${u?`<img src="${u}">`:'<div class="mediaPh">📷</div>'}<span class="copy"><b>${esc(x.room||'现场')}</b><small>${esc([x.date,x.note].filter(Boolean).join(' · '))}</small></span></button>`}$('#controlKicker').textContent='Site Evidence';$('#controlTitle').textContent='现场照片';$('#controlDesc').textContent='独立于施工日志保存的现场影像。';$('#controlList').innerHTML=`<button class="controlAdd" id="frpAdd">＋ 上传现场照片</button><div class="mediaGrid">${rows||'<div class="empty" style="grid-column:1/-1">还没有现场