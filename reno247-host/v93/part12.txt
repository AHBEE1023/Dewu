照片</div>'}</div>`;$('#frpAdd').onclick=()=>roomPhotoForm();$$('[data-fphoto]').forEach(b=>b.onclick=()=>roomPhotoDetailFull(b.dataset.fphoto))})();return}
    if(tab==='inspiration'){inspirationView();return}
    if(tab==='plan'){floorPlanView();return}
    if(tab==='contractors'){let arr=s.contractors;$('#controlKicker').textContent='Contractor × Promise';$('#controlTitle').textContent='师傅 / 供应商';$('#controlDesc').textContent='联系方式、报价、付款与关键承诺统一保留。';$('#controlList').innerHTML=`<button class="controlAdd" id="fcAdd">＋ 新增师傅 / 供应商</button>${arr.map(c=>`<button class="row" data-fcontractor="${c.id}"><span>👷</span><span class="main"><b>${esc(c.name)}</b><small>${esc([c.type||c.trade,c.room,c.contact||c.phone,c.note].filter(Boolean).join(' · '))}</small></span>${pill(c.status||'未开始')}</button>`).join('')||empty()}`;$('#fcAdd').onclick=()=>contractorFormFull();$$('[data-fcontractor]').forEach(b=>b.onclick=()=>contractorDetailFull(b.dataset.fcontractor));return}
    if(tab==='quotes'){let groups={};s.quotes.forEach(q=>(groups[q.compareGroup||q.title||'其他']??=[]).push(q));$('#controlKicker').textContent='Quote Compare';$('#controlTitle').textContent='报价比较';$('#controlDesc').textContent='候选报价不会误算进预算，选择后才计入。';$('#controlList').innerHTML=`<button class="controlAdd" id="fqAdd">＋ 新增报价</button>${Object.entries(groups).map(([g,arr])=>`<div class="shopGroupTitle"><b>${esc(g)}</b><small>${arr.length} 份</small></div>${arr.map(q=>`<button class="quoteRow ${['已选','已确认'].includes(q.status)?'selected':''}" data-fquote2="${q.id}"><div class="topline"><b>${esc(q.vendor||q.title)}</b><b>${rm(q.total||q.quote)}</b></div><small>${esc([q.room,q.status,q.note].filter(Boolean).join(' · '))}</small></button>`).join('')}`).join('')||empty()}`;$('#fqAdd').onclick=()=>quoteFormFull();$$('[data-fquote2]').forEach(b=>b.onclick=()=>quoteDetailFull(b.dataset.fquote2));return}
    if(tab==='payments'){let arr=s.payments;$('#controlKicker').textContent='Deposit × Progress × Balance';$('#controlTitle').textContent='付款记录';$('#controlDesc').textContent='真实现金流，不等同于报价。';$('#controlList').innerHTML=`<button class="controlAdd" id="fpAdd">＋ 记录付款</button>${arr.map(p=>`<button class="row" data-fpayment="${p.id}"><span>💳</span><span class="main"><b>${esc(p.title||p.vendor||p.kind||'付款')}</b><small>${esc([p.vendor,p.kind,p.date,p.note].filter(Boolean).join(' · '))}</small></span><span class="amount">${rm(p.amount)}</span></button>`).join('')||empty()}`;$('#fpAdd').onclick=()=>paymentFormFull();$$('[data-fpayment]').forEach(b=>{let p=s.payments.find(x=>x.id===b.dataset.fpayment);b.onclick=()=>paymentFormFull(p)});return}
    if(tab==='schedule'){let arr=[...s.schedule].sort((a,b)=>(a.start||'9999').localeCompare(b.start||'9999'));$('#controlKicker').textContent='施工日程';$('#controlTitle').textContent='师傅施工安排';$('#controlDesc').textContent='计划、已确认、施工中和完成。';$('#controlList').innerHTML=`<button class="controlAdd" id="fsAdd">＋ 新增施工日程</button>${arr.map(x=>`<button class="row" data-fschedule="${x.id}"><span>📅</span><span class="main"><b>${esc(x.title)}</b><small>${esc([x.start,x.end,x.room,x.vendor].filter(Boolean).join(' · '))}</small></span>${pill(x.status||'计划')}</button>`).join('')||empty()}`;$('#fsAdd').onclick=()=>scheduleFormFull();$$('[data-fschedule]').forEach(b=>{let x=s.schedule.find(v=>v.id===b.dataset.fschedule);b.onclick=()=>scheduleFormFull(x)});return}
    if(tab==='timeline'){let a=timelineItems();$('#controlKicker').textContent='Auto Timeline';$('#controlTitle').textContent='装修时间线';$('#controlDesc').textContent='施工、任务、验收、付款和采购日期自动汇总。';$('#controlList').innerHTML=`<div class="timeline">${a.map(x=>`<div class="timelineItem"><b>${x.ico} ${esc(x.date)} · ${esc(x.title)}</b><small>${esc(x.sub||'')}</small></div>`).join('')||'<div class="empty">还没有有日期的事项</div>'}</div>`;return}
    if(tab==='reminder'){let arr=s.paymentReminders;$('#controlKicker').textContent='Payment Reminder';$('#controlTitle').textContent='付款提醒';$('#controlDesc').textContent='提醒与真实付款记录分开。';$('#controlList').innerHTML=`<button class="controlAdd" id="fprAdd">＋ 新增付款提醒</button>${arr.map(x=>`<button class="row" data-freminder="${x.id}"><span>⏰</span><span class="main"><b>${esc(x.title)}</b><small>${esc([x.vendor,x.due,x.note].filter(Boolean).join(' · '))}</small></span><span class="amount">${rm(x.amount)}</span>${pill(x.status||'待付款')}</button>`).join('')||empty()}`;$('#fprAdd').onclick=()=>reminderFormFull();$$('[data-freminder]').forEach(b=>{let x=s.paymentReminders.find(v=>v.id===b.dataset.freminder);b.onclick=()=>reminderFormFull(x)});return}
    if(tab==='handover'){let arr=s.handover;$('#controlKicker').textContent='Handover × Warranty';$('#controlTitle').textContent='交付 / 保固';$('#controlDesc').textContent='工程结束后仍然有价值的房屋档案。';$('#controlList').innerHTML=`<button class="controlAdd" id="fhAdd">＋ 添加交付 / Warranty</button>${arr.map(x=>`<button class="row" data-fhandover="${x.id}"><span>🔑</span><span class="main"><b>${esc(x.title)}</b><small>${esc([x.vendor,x.expiry?('保固至 '+x.expiry):'',x.note].filter(Boolean).join(' · '))}</small></span>${pill(x.status||'待收齐')}</button>`).join('')||empty()}`;$('#fhAdd').onclick=handoverForm;$$('[data-fhandover]').forEach(b=>b.onclick=()=>handoverDetailFull(b.dataset.fhandover));return}
    control922();
  };

  // ---------- QUICK ADD: restore old functions, keep all new ones ----------
  const quickBox=document.querySelector('#quick .quick');if(quickBox){quickBox.classList.add('fullQuick');const items=[['task','✅','任务'],['decision','◇','待确认'],['shopping','🛒','商品'],['defect','⚠️','Defect'],['idea','🖼️','灵感'],['plan','📐','Floor Plan'],['contractor','👷','师傅'],['expense','💰','费用'],['variation','±','变更'],['log','📝','日志'],['inspect','✓','验收'],['schedule','📅','日程']];quickBox.innerHTML=items.map(x=>`<button data-fullquick="${x[0]}">${x[1]}<small>${x[2]}</small></button>`).join('');$$('[data-fullquick]').forEach(b=>b.onclick=()=>{let q=b.dataset.fullquick;$('#quick').classList.add('hide');if(q==='task')add('task');if(q==='decision')decisionFormFull();if(q==='shopping')productForm();if(q==='defect')defectFormFull();if(q==='idea')inspirationForm();if(q==='plan')floorPlanView();if(q==='contractor')contractorFormFull();if(q==='expense')expenseFormFull();if(q==='variation')add('variation');if(q==='log')siteLogFormFull();if(q==='inspect')add('inspect');if(q==='schedule')scheduleFormFull()})}

  // ---------- MY: backup import restore ----------
  const exportRow=$('#export');if(exportRow&&!$('#importBackupFull')){let b=document.createElement('button');b.className='row';b.id='importBackupFull';b.innerHTML='⇧ <span class="main"><b>导入备份</b><small>恢复 Reno247 JSON，不清空现有图片数据库</small></span>';exportRow.insertAdjacentElement('afterend',b);b.onclick=()=>{let f=document.createElement('input');f.type='file';f.accept='.json,application/json';f.onchange=()=>{let file=f.files?.[0];if(!file)return;let r=new FileReader();r.onload=()=>{try{let data=JSON.parse(r.result);if(confirm('用这个 JSON 替换当前文字资料？图片数据库不会删除。')){s={...data};for(const k of extraArrays)if(!Array.isArray(s[k]))s[k]=[];save();location.reload()}}catch(e){alert('备份文件格式不正确')}};r.readAsText(file)};f.click()}}

  // Notification bell becomes useful again.
  $('#bell').onclick=()=>{let a=nextActions(),rem=s.paymentReminders.filter(x=>x.status!=='已付款'&&x.due&&dateDelta(x.due)<=7).sort((x,y)=>(x.due||'').localeCompare(y.due||''));modal('装修提醒','优先显示会影响工程和近期付款',`${a.slice(0,5).map(x=>`<div class="row"><span>${x.icon}</span><span class="main"><b>${esc(x.title)}</b><small>${esc(x.sub||'')}</small></span>${pill(x.status)}</div>`).join('')||empty()}${rem.length?`<section class="section"><h3>7天内付款</h3>${rem.map(x=>`<div class="row"><span>💳</span><span class="main"><b>${esc(x.title)}</b><small>${esc(x.due||'')}</small></span><span class="amount">${rm(x.amount)}</span></div>`).join('')}</section>`:''}`)};

  // Global search: a full V9.3 search that includes restored modules.
  function fullOpenGlobalSearch(q=''){
    modal('搜索装修资料','房间、商品、任务、决定、Defect、电位、师傅、灵感、报价和日程一起搜',`<label>搜索<input id="fgsq" autocomplete="off" placeholder="例如：冷气 / 厨房 / 插座 / 橱柜" value="${esc(q)}"></label><div id="fgsResults" class="searchResults"></div>`);
    const inp=$('#fgsq'),run=()=>fullRenderGlobalSearch(inp.value.trim());inp.oninput=run;setTimeout(()=>{inp.focus();run()},30)
  }
  function fullRenderGlobalSearch(q){
    const box=$('#fgsResults');if(!box)return;if(!q){box.innerHTML='<div class="empty">输入关键词即可跨所有装修资料搜索</div>';return}const z=q.toLowerCase(),hit=v=>String(v||'').toLowerCase().includes(z),groups=[];
    const push=(name,arr)=>{if(arr.length)groups.push([name,arr])};
    push('房间',s.rooms.filter(r=>hit(r)||hit(s.roomData[r]?.note)||hit(s.roomData[r]?.dimensions)).slice(0,8).map(r=>({ico:EM[r]||'🏠',title:r,sub:[s.roomData[r]?.dimensions,s.roomData[r]?.note].filter(Boolean).join(' · '),kind:'room',id:r})));
    push('选购',s.shopping.filter(x=>[x.item,x.room,x.category,x.note,x.platform,x.group].some(hit)).slice(0,10).map(x=>({ico:'🛒',title:x.item,sub:[x.room,fullPhaseOf(x),x.price?rm(landed(x)):'待补价格'].filter(Boolean).join(' · '),kind:'shop',id:x.id})));
    push('任务',s.tasks.filter(x=>[x.name,x.room,x.note].some(hit)).slice(0,8).map(x=>({ico:'✅',title:x.name,sub:x.room||x.note,kind:'tab',id:'task'})));
    push('决定',s.decisions.filter(x=>[x.title,x.room,x.note,x.block].some(hit)).slice(0,8).map(x=>({ico:'◇',title:x.title,sub:x.block?'阻塞：'+x.block:(x.room||x.note),kind:'decision',id:x.id})));
    push('Defect',s.defects.filter(x=>[x.title,x.room,x.note,x.owner].some(hit)).slice(0,8).map(x=>({ico:'⚠️',title:x.title,sub:x.room||x.note,kind:'defect',id:x.id})));
    push('电位',s.sockets.filter(x=>[x.title,x.room,x.position,x.note,x.type].some(hit)).slice(0,8).map(x=>({ico:'🔌',title:x.title,sub:[x.room,x.position,x.qty].filter(Boolean).join(' · '),kind:'tab',id:'sockets'})));
    push('师傅',s.contractors.filter(x=>[x.name,x.type,x.trade,x.contact,x.phone,x.note].some(hit)).slice(0,8).map(x=>({ico:'👷',title:x.name,sub:[x.type||x.trade,x.contact||x.phone].filter(Boolean).join(' · '),kind:'contractor',id:x.id})));
    push('灵感',s.ideas.filter(x=>[x.title,x.room,x.note].some(hit)).slice(0,8).map(x=>({ico:'🖼️',title:x.title||'灵感',sub:x.room||x.note,kind:'idea',id:x.id})));
    push('Floor Plan',s.markers.filter(x=>[x.label,x.room,x.type,x.note].some(hit)).slice(0,8).map(x=>({ico:'📍',title:x.label,sub:[x.room,x.type].filter(Boolean).join(' · '),kind:'plan',id:x.id})));
    push('报价',s.quotes.filter(x=>[x.title,x.vendor,x.room,x.note,x.compareGroup].some(hit)).slice(0,8).map(x=>({ico:'🧾',title:x.vendor||x.title,sub:rm(x.total||x.quote),kind:'quote',id:x.id})));
    push('日程',s.schedule.filter(x=>[x.title,x.room,x.vendor,x.note].some(hit)).slice(0,8).map(x=>({ico:'📅',title:x.title,sub:[x.start,x.room,x.vendor].filter(Boolean).join(' · '),kind:'schedule',id:x.id})));
    if(!groups.length){box.innerHTML='<div class="empty">没有找到相关资料</div>';return}
    box.innerHTML=groups.map(([g,arr])=>`<div class="searchGroup"><small>${g} · ${arr.length}</small>${arr.map(x=>`<button class="searchItem" data-fkind="${x.kind}" data-fid="${esc(x.id)}"><span class="sico">${x.ico}</span><span class="scopy"><b>${esc(x.title)}</b><small>${esc(x.sub||'')}</small></span><span>›</span></button>`).join('')}</div>`).join('');
    $$('[data-fkind]').forEach(b=>b.onclick=()=>{let k=b.dataset.fkind,id=b.dataset.fid;if(k==='room')roomHubFull(id);else if(k==='shop')productDetail(id);else if(k==='decision')decisionDetailFull(id);else if(k==='defect')defectDetailFull(id);else if(k==='contractor')contractorDetailFull(id);else if(k==='idea')inspirationDetail(id);else if(k==='plan')floorPlanView();else if(k==='quote')quoteDetailFull(id);else if(k==='schedule'){let x=s.schedule.find(v=>v.id===id);scheduleFormFull(x)}else if(k==='tab'){tab=id;$('#modal').classList.add('hide');go('control');fullSetControlActive(id);renderControl()}})
  }
  const gsb=$('#globalSearchBtn');if(gsb)gsb.onclick=()=>fullOpenGlobalSearch();

  // Rewrap render so content-aware enhancements remain